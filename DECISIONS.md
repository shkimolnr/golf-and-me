# Golf & Me 결정 기록

이 문서는 확정된 제품·기술 결정을 시간순으로 보관합니다. 새로운 결정은 기존 내용을 지우지 않고 다음 번호로 추가하며, 번복 시 대체한 결정 번호를 명시합니다.

## 결정 요약

| ID | 날짜 | 결정 | 상태 |
|---|---|---|---|
| DEC-001 | 2025-09 | 모바일 웹(PWA) 우선 | 확정 |
| DEC-002 | 2025-09 | React + Tailwind 사용 | DEC-042로 대체 |
| DEC-003 | 2025-09 | Supabase 유지 | 확정 |
| DEC-004 | 2025-09 | Google 로그인 우선 | 확정 |
| DEC-005 | 2025-09 | 골프장·야디지 없이도 MVP 동작 | 확정 |
| DEC-006 | 2025-09 | 비순차 홀 입력 허용 | 확정 |
| DEC-007 | 2025-09 | 라운드 중 기록, 라운드 후 분석 중심 | 확정 |
| DEC-008 | 2026-08-24 | 5개 관리 문서 역할 분리 | 확정 |
| DEC-009 | 2026-08-24 | 클럽 거리 이력 정규화 권장 | 제안 |
| DEC-010 | 2026-08-24 | Alpha 빠른 실행 기본값 | 임시 확정 |
| DEC-011 | 2026-08-26 | 프로필 아이콘은 계정 메뉴를 열도록 구성 | 확정 |
| DEC-012 | 2026-08-26 | Alpha 온보딩 정보는 브라우저에 임시 저장 | 임시 확정 |
| DEC-013 | 2026-08-26 | Griun Cherrie를 기본 브랜드 서체로 사용 | 확정 |
| DEC-014 | 2026-08-26 | Alpha 진행 중 라운드는 브라우저에 임시 저장 | 임시 확정 |
| DEC-015 | 2026-08-28 | Alpha 골프장 자동완성은 개인 입력 이력으로 시작 | 임시 확정 |
| DEC-016 | 2026-08-28 | 골프장 정보가 없으면 홀 파를 추정하지 않음 | 확정 |
| DEC-017 | 2026-08-28 | 홀 결과는 샷 기록에서 자동 계산 | 일부 대체 |
| DEC-018 | 2026-08-28 | 엑셀 골프장 마스터를 앱용 정적 DB로 변환 | 확정 |
| DEC-019 | 2026-08-28 | 작성 중 라운드는 여러 건 보관 | 임시 확정 |
| DEC-020 | 2026-08-29 | 공식 홀번호와 플레이 순서를 분리 | 확정 |
| DEC-021 | 2026-08-29 | UI에 그림자 효과를 사용하지 않음 | 확정 |
| DEC-022 | 2026-08-29 | 실제 샷과 벌타를 분리하여 계산 | 확정 |
| DEC-023 | 2026-08-29 | 작성 중·완료 라운드 삭제를 허용 | 확정 |
| DEC-024 | 2026-08-29 | 퍼팅 시작 위치로 마지막 샷 결과를 확정 | 확정 |
| DEC-025 | 2026-08-29 | 홀 입력 화면의 현재 작업을 단계적으로 강조 | 확정 |
| DEC-026 | 2026-08-30 | 작성 중 저장과 완료 홀 수정을 분리 | 확정 |
| DEC-027 | 2026-08-30 | 모바일 중단 후 마지막 기록 화면 복원 | 확정 |
| DEC-028 | 2026-08-30 | 완료 라운드는 결과 화면을 먼저 표시 | 임시 확정 |
| DEC-029 | 2026-08-30 | 라운드 구조 정보는 초기 3개 홀까지만 변경 | 확정 |
| DEC-030 | 2026-08-30 | 스코어카드 뒤로가기와 정보 수정을 분리 | 확정 |
| DEC-031 | 2026-08-30 | 홀 저장과 라운드 완료를 분리 | 확정 |
| DEC-032 | 2026-08-30 | 통계 결측값은 분모에서 제외하고 표본 수를 보존 | 확정 |
| DEC-033 | 2026-08-30 | 라운드는 로컬 우선 저장 후 Supabase와 병합 | 임시 확정 |
| DEC-034 | 2026-08-30 | 로그아웃은 현재 기기 세션만 종료 | 확정 |
| DEC-035 | 2026-08-30 | 계정 전환 시 화면 상태를 사용자별로 격리 | 확정 |
| DEC-036 | 2026-08-30 | 선택 티 거리 결측 안내는 입력 전후 역할을 분리 | 확정 |
| DEC-037 | 2026-08-30 | 클럽 비거리는 시점별 전체 세트로 보존 | 확정 |
| DEC-038 | 2026-08-30 | 클럽 구성과 비거리 이력은 계정 단위로 병합·보존 | 확정 |
| DEC-039 | 2026-08-30 | 홈 누적 통계는 완료 기록의 유효 표본을 합산 | 확정 |
| DEC-040 | 2026-08-30 | 날씨·감정·이메일은 Alpha 기록 흐름에서 제외 | 확정 |
| DEC-041 | 2026-08-30 | Alpha 품질 검증은 실제 기기 확인과 자동 회귀를 함께 사용 | 확정 |
| DEC-042 | 2026-08-30 | React·Vite·CSS와 OpenAI Sites로 HTTPS Alpha 임시 운영 | DEC-044로 대체 |
| DEC-043 | 2026-08-30 | 초기 정책 변경은 사용자 명시 결정 후 실행 | 확정 |
| DEC-044 | 2026-08-30 | 정식 배포 경로를 Vercel로 복구 | 확정 |
| DEC-045 | 2026-08-30 | OpenAI Sites 레거시 배포와 인증 주소 종료 | 확정 |
| DEC-046 | 2026-08-30 | 계정 탈퇴 시 본인 계정과 전체 기록을 영구 삭제 | 확정 |
| DEC-047 | 2026-08-30 | Free Alpha는 수동 DB 백업과 복구 모의훈련을 사용 | 확정 |
| DEC-048 | 2026-08-30 | 퍼팅 입력과 홀 완료를 분리 | 확정 |
| DEC-049 | 2026-08-30 | 라운드 클럽은 당시 ID와 표시명 스냅샷으로 보존 | 확정 |
| DEC-050 | 2026-08-30 | 최초 라운드 전 실제 클럽 구성을 필수 확인 | 확정 |
| DEC-051 | 2026-08-30 | Beta 행동 분석은 비식별 허용 이벤트로 준비 | DEC-060으로 대체 |
| DEC-052 | 2026-08-31 | Beta 요청과 의견은 Slack 접수·전체 새소식으로 가볍게 운영 | 확정 |
| DEC-053 | 2026-08-31 | 로그인 속도는 현재 통과 후 GA 실사용 추세로 재검증 | DEC-060으로 일부 대체 |
| DEC-058 | 2026-08-31 | 원격 조회 실패 원인은 분리 측정 후 확장성 구조를 결정 | 확정 |
| DEC-059 | 2026-08-31 | 오류 관측은 MVP 최소 범위 후 풀스펙으로 확장 | DEC-060으로 대체 |
| DEC-060 | 2026-08-31 | GA4 제품 분석과 Supabase 운영 진단 분리 | 확정 |
| DEC-061 | 2026-09-01 | 완료 라운드는 요약 우선, 상세 원본은 필요할 때 로드 | 확정 |
| DEC-062 | 2026-09-01 | UI worktree는 최신 검증 기준에서 짧게 운영 | 확정 |
| DEC-063 | 2026-09-01 | 골프장 기준 거리와 실제 플레이 거리를 분리 보존 | 확정 |

## DEC-001 — 모바일 웹(PWA) 우선

- 상태: 확정
- 맥락: 초기 제품은 스마트폰 접근성과 빠른 개발·배포가 중요합니다.
- 결정: 네이티브 앱보다 모바일 웹(PWA)을 먼저 개발합니다.
- 결과: 모든 핵심 UX와 검증은 모바일 화면을 우선합니다. 네이티브 앱 전환은 Phase 4에서 다시 검토합니다.

## DEC-002 — React + Tailwind 사용

- 상태: DEC-042로 대체
- 맥락: 초기 UI를 빠르게 반복하고 재사용 가능한 화면 구조가 필요합니다.
- 결정: 프론트엔드는 React와 Tailwind CSS를 사용합니다.
- 결과: 공통 디자인 토큰과 컴포넌트를 먼저 정의합니다.

## DEC-003 — Supabase 유지

- 상태: 확정
- 맥락: 인증, PostgreSQL 기반 통계, 서버 기능을 빠르게 구축해야 하며 초기 규모에서 GCP 직접 구성은 복잡도가 큽니다.
- 결정: 인증, 데이터베이스, 서버 기능의 기본 플랫폼으로 Supabase를 사용합니다.
- 결과: 사용자 데이터에는 RLS를 필수 적용하고 성장 단계에 따라 요금제를 재검토합니다.

## DEC-004 — Google 로그인 우선

- 상태: 확정
- 맥락: MVP 개발 속도를 우선하면서 로그인 방식을 단순화해야 합니다.
- 결정: MVP는 Google OAuth 2.0만 제공합니다.
- 결과: 카카오 로그인은 Phase 3 후보로 둡니다.

## DEC-005 — 골프장·야디지 없이도 MVP 동작

- 상태: 확정
- 맥락: 완전한 국내 골프장 및 티별 야디지 데이터 확보는 초기 범위를 크게 늘립니다.
- 결정: MVP의 골프장과 코스는 수동 입력이며 야디지는 선택값입니다.
- 결과: 거리 데이터가 없어도 라운드 생성, 홀 기록, 요약이 모두 동작해야 합니다.

## DEC-006 — 비순차 홀 입력 허용

- 상태: 확정
- 맥락: 실제 라운드에서는 입력을 놓치거나 나중에 수정할 수 있습니다.
- 결정: 홀 순서를 강제하지 않고 모든 홀을 언제든 입력·수정할 수 있게 합니다.
- 결과: 스코어카드가 미입력과 입력 완료 상태를 분명히 보여야 합니다.

## DEC-007 — 라운드 중 기록, 라운드 후 분석 중심

- 상태: 확정
- 맥락: 라운드 중 복잡한 입력과 조언은 플레이 집중을 방해할 수 있습니다.
- 결정: 라운드 중에는 핵심 데이터의 빠른 기록을, 종료 후에는 통계와 패턴 분석을 제공합니다.
- 결과: 실시간 AI 조언은 MVP에서 제외하고 Phase 3 후보로 둡니다.

## DEC-008 — 5개 관리 문서 역할 분리

- 상태: 확정
- 맥락: 요구사항, 실행 상태, 결정 근거가 한 문서에 섞이면 변경 추적이 어렵습니다.
- 결정: `README.md`, `PROJECT_RULES.md`, `PRD.md`, `BACKLOG.md`, `DECISIONS.md`의 역할을 분리합니다.
- 결과: 변경 종류에 맞는 문서를 갱신하고, 서로 모순되는 상태를 남기지 않습니다.

## DEC-009 — 클럽 거리 이력 정규화 권장

- 상태: 제안
- 맥락: 원본 PRD는 `clubs.history` 배열을 제시하지만, 이력 검색과 확장에는 별도 테이블이 유리합니다.
- 제안: 현재 거리와 이력을 `clubs` 및 `club_distance_history`로 분리합니다.
- 영향: 마이그레이션과 통계 조회가 명확해지지만 테이블 수가 하나 늘어납니다.
- 확정 조건: 초기 DB 스키마 설계 시 승인합니다.

## 미결정 대기열

다음 항목은 아직 결정이 아니며, 확정 후 새 DEC 번호를 부여합니다.

- 라운드 완료 및 완료 후 수정 정책 → `DEC-031`에서 확정
- FIR/GIR/퍼팅 결측값의 통계 분모 정책
- 날씨 API와 위치 권한 정책
- 감정 체크 MVP 포함 여부
- 이메일 알림 시나리오
- 불안정 네트워크에서의 임시 저장 방식

## DEC-010 — Alpha 빠른 실행 기본값

- 상태: 임시 확정
- 맥락: 정책 논의를 모두 확정한 뒤 개발하면 실제 사용 가능한 Alpha 검증이 늦어집니다.
- 결정: 되돌리기 쉬운 기본값으로 핵심 기록 흐름을 먼저 구현하고, 사용 검증 결과로 정책을 조정합니다.
- 기본값: 진행 중 라운드는 입력된 홀만 저장할 수 있고, 18홀의 스윙 횟수가 모두 입력되면 완료할 수 있습니다.
- 기본값: 완료 후에도 수정할 수 있으며 수정 즉시 요약과 누적 통계를 재계산합니다.
- 기본값: FIR·GIR·퍼팅 미입력값은 통계 분모에서 제외하고, 표본이 없으면 수치 대신 `-`를 표시합니다.
- 기본값: 파3의 FIR은 `null`이며, 날씨·감정 체크·이메일·클럽 거리 관리는 Alpha 핵심 흐름 이후로 미룹니다.
- 영향: 정책 확정 작업은 Alpha 선행 작업이 아니며, 구현 중 치명적인 충돌이 발견될 때만 즉시 재검토합니다.

## DEC-011 — 프로필 아이콘은 계정 메뉴를 열도록 구성

- 상태: 확정
- 맥락: 프로필 이니셜을 눌렀을 때 즉시 로그아웃하면 동작을 예측하기 어렵고 실수로 세션이 종료될 수 있습니다.
- 결정: 프로필 이니셜 또는 이미지는 모바일 바텀시트 계정 메뉴를 열며, 메뉴에 사용자 이름, Google 이메일과 명시적인 로그아웃 버튼을 표시합니다.
- 결과: 로그아웃은 계정 메뉴 안에서만 실행하고, 설정 아이콘은 기본 티그라운드와 클럽별 거리 관리 화면 구현 시 연결합니다.

## DEC-012 — Alpha 온보딩 정보는 브라우저에 임시 저장 (서버 동기화로 대체)

- 상태: 대체됨
- 맥락: 데이터 모델 변경은 보류된 상태이며, 최초 로그인과 기본 티 설정 흐름을 먼저 검증해야 합니다.
- 결정: 사용자별 온보딩 완료 여부와 기본 티를 브라우저 로컬 저장소에 임시 저장합니다.
- 결과: 동일 브라우저 로컬 저장을 안전망으로 유지하되, 현재는 Supabase `profiles`에 기본 티·거리 단위와 온보딩 완료 여부를 계정별 동기화합니다.

## DEC-013 — Griun Cherrie를 기본 브랜드 서체로 사용

- 상태: 확정
- 맥락: 제품의 아날로그 노트 감성과 고유한 브랜드 인상을 강화할 직접 제작 서체가 제공되었습니다.
- 결정: `Griun Cherrie`를 앱 전반의 기본 서체로 적극 사용하고 시스템 한글 폰트를 대체 서체로 지정합니다.
- 결과: Alpha에서는 TTF 원본을 사용하며, 출시 성능 최적화 단계에서 WOFF2 변환과 서브셋 적용을 검토합니다.

## DEC-014 — Alpha 진행 중 라운드는 브라우저에 임시 저장

- 상태: 임시 확정
- 맥락: 데이터 모델 변경을 보류하면서 라운드 생성부터 홀 기록까지의 핵심 흐름을 먼저 검증해야 합니다.
- 결정: 사용자별 진행 중 라운드 한 건과 기본 18홀을 브라우저 로컬 저장소에 임시 저장합니다.
- 결과: 새로고침 후에도 진행 중 라운드를 이어갈 수 있으며, Supabase 스키마 확정 시 서버 저장으로 교체합니다.

## DEC-015 — Alpha 골프장 자동완성은 개인 입력 이력으로 시작

- 상태: 임시 확정
- 맥락: 공식 골프장 DB가 없는 상태에서도 반복 입력을 줄이되, 오타와 중복이 공용 데이터로 바로 노출되는 문제를 피해야 합니다.
- 결정: 사용자별 최근 골프장명과 전반·후반 코스명 조합을 브라우저에 최대 20개 저장하고 자동완성 후보로 제공합니다. 코스명은 IN/OUT으로 제한하지 않고 골프장 고유 명칭을 허용합니다.
- 결과: Alpha에서는 동일 사용자와 브라우저 안에서만 자동완성이 동작합니다. 공용 후보 축적·검증·별칭 병합은 Supabase 골프장 모델을 설계할 때 추가하며, 각 라운드에는 입력 당시 명칭을 스냅샷으로 보존합니다.

## DEC-016 — 골프장 정보가 없으면 홀 파를 추정하지 않음

- 상태: 확정
- 맥락: 공식 코스 DB가 없는 상태에서 일반적인 파 배열을 적용하면 실제 홀 정보와 다른 기록이 만들어지며, 드물게 존재하는 파6도 표현할 수 없습니다.
- 결정: 새 홀의 파는 `null`로 생성하고 전반 1~9번·후반 10~18번으로 표시합니다. 홀 상세에서는 PAR를 첫 필수 작업으로 안내하고 파3·파4·파5·파6 중 직접 선택하며, 미입력 홀 거리는 `—`로 표시합니다.
- 결과: 파가 확인된 홀만 파 대비 계산에 포함하고, 파3 선택 시 FIR은 적용 대상 아님(`null`)으로 처리합니다. 동일 코스의 파 정보를 재사용하는 개인 코스 템플릿은 후속 작업으로 둡니다.

## DEC-017 — 홀 결과는 샷 기록에서 자동 계산

- 상태: 일부 대체 — 벌타 계산은 `DEC-022`가 대체
- 맥락: 사용자가 스윙 수·FIR·GIR·벌타 결과를 직접 입력하면 클럽 선택과 트러블 원인을 분석할 수 없습니다.
- 결정: 홀마다 TEE와 최소 두 개의 후속 샷 행을 제공하고, 사용 클럽·남은 거리·트러블만 기록합니다. 트러블 미입력은 가운데·페어웨이로 해석하며 좌우 방향과 러프·벙커·페널티·OB만 예외로 입력합니다.
- 결과: 스코어·FIR·GIR·벌타는 샷과 퍼팅 기록에서 자동 계산합니다. 구체적인 OB·페널티 계산은 `DEC-022`를 따르며, 퍼팅 세부 데이터는 후속 검토합니다.

## DEC-018 — 엑셀 골프장 마스터를 앱용 정적 DB로 변환

- 상태: 확정
- 맥락: 개인 테스트 단계에서는 사용자가 자주 가는 골프장의 공식 야디지를 월 5~8곳 정도 직접 수집하며, 엑셀이 가장 편한 관리 도구입니다.
- 결정: 한 시트 누적 엑셀을 운영 원본으로 유지하고 앱에는 정규화한 JSON 스냅샷을 포함합니다. 별칭, 대표 출처, 최초 수집일, 마지막 확인일을 관리하며 엑셀의 `없음`은 앱에서 `null`로 변환합니다.
- 결정: 이름 검색 결과에 `[홀 정보]`를 표시하되 사용자가 후보를 명시적으로 선택한 경우에만 연결합니다. 연결 후 코스·티·단위를 기준으로 PAR와 거리를 자동 입력하며 사용자가 수정할 수 있습니다.
- 결정: `[홀 정보]`를 선택한 새 라운드는 생성 직전에 골프장명·전후반 코스·티·단위를 한 번 확인합니다. 세 번째 플레이 홀까지 구조 정보를 변경할 수 있고 이후에는 새 라운드가 필요하다는 안내를 함께 표시하되, 기존 라운드의 정보 수정 저장에는 반복하지 않습니다.
- 결정: 연결된 라운드의 티·코스·단위를 수정하면 DB에서 자동 입력된 PAR·홀 거리만 일괄 갱신합니다. 사용자가 직접 수정한 값과 티샷 이후의 실제 플레이 기록은 보존하며, 선택 티의 공식 거리가 없으면 이전 티 값을 남기지 않고 `0`으로 둡니다.
- 결과: 공식 정보가 없는 골프장은 기존 자유 입력과 PAR 직접 선택 흐름을 유지하고, 수집된 골프장은 입력량을 줄입니다.

## DEC-019 — 작성 중 라운드는 여러 건 보관

- 상태: 임시 확정
- 맥락: 실제 라운드는 18홀을 한 번에 모두 입력하지 못할 수 있고, 작성 중 새 라운드를 만들 때 기존 한 건을 덮어쓰면 기록 손실이 발생합니다.
- 결정: 새 라운드는 기존 라운드를 덮어쓰지 않고 사용자별 라운드 배열에 추가합니다. 작성 중 기록과 완료 기록을 모두 보존하고 홈에서 각각 다시 열 수 있게 합니다.
- 결과: 대시보드 구조가 확정되기 전까지 홈에는 `작성 중인 기록`과 `완료한 기록`의 간단한 카드 목록을 사용합니다. 삭제·보관·완료 전환 정책은 후속 시뮬레이션에서 결정합니다.

## DEC-020 — 공식 홀번호와 플레이 순서를 분리

- 상태: 확정
- 맥락: 홀번호는 골프장 코스에 속한 고유 정보이며, IN 코스를 전반에 선택하면 실제 플레이는 공식 10번 홀부터 시작할 수 있습니다.
- 결정: 골프장 마스터의 기존 `홀` 열에는 원본 공식 홀번호를 그대로 저장하고 임의로 1~9로 재번호화하지 않습니다. 라운드에는 계산과 배열을 위한 플레이 순서 1~18을 별도로 둡니다.
- 결과: 스코어카드는 선택한 코스의 공식 홀번호를 표시합니다. IN→OUT은 `10~18 → 1~9`, OUT→IN은 `1~9 → 10~18`로 보이며 코스 자체가 1~9를 사용하는 경우에는 원본 번호를 그대로 사용합니다.
- 보완: 플라자CC 용인의 TIGER·LION처럼 각 코스가 공식 1~18번을 모두 가진 경우, 원본 코스명과 홀번호는 그대로 보존합니다. 앱에서만 `TIGER OUT`(1~9), `TIGER IN`(10~18)처럼 선택 구간을 파생하며 서로 다른 구간 조합도 허용합니다. 기존에 원본 코스명만 저장된 기록은 플레이 위치에 따라 전반 1~9·후반 10~18로 호환합니다.

## DEC-021 — UI에 그림자 효과를 사용하지 않음

- 상태: 확정
- 맥락: 버튼·패널·카드에 그림자를 혼용하면 손글씨 서체와 평면적인 기록지 화면보다 장식 효과가 먼저 보이고 컴포넌트 간 규칙도 흐려집니다.
- 결정: 버튼, 카드, 자동완성 목록, 바텀시트와 데스크톱 앱 프레임을 포함해 UI에 `box-shadow`와 유사한 그림자 효과를 사용하지 않습니다.
- 결과: 선택·포커스·레이어 구분은 테두리, 배경색, 간격과 명암 대비로 표현합니다.

## DEC-022 — 실제 샷과 벌타를 분리하여 계산

- 상태: 확정
- 맥락: OB는 재샷과 전진 구제 모두 정상 플레이 대비 두 타의 영향을 줄 수 있지만, 실제 샷과 벌타의 구성은 다릅니다. 샷 결과에 일괄 +2를 붙이면 재샷을 별도 샷으로 기록할 때 중복 계산됩니다.
- 결정: 스코어는 `스코어에 포함되는 실제 샷 수 + 퍼팅 수 + 벌타 합계`로 계산합니다. 페널티 구역은 화면에서 `페널티`로 표시하고 +1벌타, OB 재샷은 실제 재샷과 +1벌타, OB 전진 구제는 +2벌타로 기록합니다.
- 결정: OB 재샷을 선택하면 재샷 위치의 거리를 자동 입력합니다. 티샷 재샷은 홀거리, 이후 샷 재샷은 해당 샷의 남은거리를 사용합니다. 원본 거리가 바뀌면 자동값도 함께 갱신하지만 사용자가 재샷 거리를 직접 수정한 뒤에는 덮어쓰지 않습니다. 전진 구제는 위치가 달라지므로 거리를 자동 입력하지 않습니다.
- 결정: 홀을 마친 뒤 일괄 입력하도록 강제하지 않고, 다음 샷을 준비할 때 직전 샷 결과와 현재 샷 거리·클럽을 기록할 수 있게 합니다. 원구가 발견되어 스코어에 포함되지 않는 잠정구는 MVP 기록에서 제외합니다.
- 결과: 상단 현재 스코어는 입력된 실제 샷과 벌타를 즉시 반영하고, 저장·재진입·스코어카드에서도 같은 계산을 사용합니다.
- 후속: 페널티 구역이 아닌 규칙 위반 벌타(라이 개선, 벙커 모래 접촉 등)는 별도 벌타 유형으로 분리해야 하며 `TASK-106`에서 검토합니다.

## DEC-023 — 작성 중·완료 라운드 삭제를 허용

- 상태: 확정
- 맥락: 잘못 생성하거나 중복된 작성 중 라운드가 쌓일 수 있고, 완료한 기록도 사용자 소유 데이터이므로 잘못 완료했거나 중복 저장한 경우 삭제할 수 있어야 합니다.
- 결정: 작성 중 기록은 홈 카드의 보조 메뉴에서 삭제할 수 있게 하고 일반 확인창을 표시합니다. 완료 기록은 실수 가능성을 줄이기 위해 상세 또는 라운드 정보 화면에서만 삭제할 수 있게 하며, 삭제 시 누적 통계에서도 제외된다는 안내를 표시합니다.
- 결정: 라운드를 삭제하면 해당 라운드와 연결된 홀 임시 저장·샷 입력 초안을 함께 제거합니다. 홀 하나는 삭제보다 수정 흐름을 사용하고, 사용자가 추가한 샷은 기존 행 삭제 기능을 유지합니다.
- 결과: Alpha 로컬 저장 단계에서는 확인 후 삭제하고 복구 기능은 제공하지 않습니다. Supabase 연결 이후에는 `deleted_at` 기반 소프트 삭제와 복구 기간 또는 휴지통 도입을 검토합니다.
- 구현: 작성 중 기록은 홈 카드의 `삭제`, 완료 기록은 스코어카드 하단의 `완료한 기록 삭제`에서 진입합니다. 확인창 승인 시 라운드와 연결된 홀 임시 저장을 로컬 저장소에서 함께 제거합니다.
- 보완: 서버 삭제가 실패해도 삭제한 기록이 다시 나타나지 않도록 사용자별 삭제 대기 ID를 기기에 보존하고, 연결 복구 후 서버 삭제를 자동 재시도합니다.

## DEC-024 — 퍼팅 시작 위치로 마지막 샷 결과를 확정

- 상태: 확정
- 맥락: 마지막으로 입력된 샷이라고 해서 곧바로 온 그린으로 볼 수 없고, 그린 엣지·에이프런에서 퍼터를 사용하는 경우도 있습니다. 반대로 퍼팅을 아직 입력하지 않은 작성 도중에는 앱이 샷 결과를 성급하게 추정하면 안 됩니다.
- 결정: 퍼팅 영역에 시작 위치 `그린/엣지`를 두고 기본값은 그린으로 합니다. 퍼팅 횟수가 선택되기 전에는 기존 샷 결과를 유지하고, 선택 후 마지막 샷을 그린이면 `온 그린`, 엣지면 `엣지`, 퍼팅 0회면 `홀인`으로 표시합니다.
- 결정: 엣지에서 선택한 퍼터 타수는 모두 총 스코어에 포함하되 첫 타는 그린 밖 스트로크로 보아 공식 퍼팅 수에서 제외합니다. 복수의 엣지 스트로크처럼 드문 경우는 후속 퍼팅 상세 모델에서 확장합니다.
- 결정: 퍼팅 수에는 임의의 최대값을 두지 않습니다. 화면에는 0~4회 빠른 선택과 5회 이상 직접 입력을 제공하며, 직접 입력값은 마지막 버튼에 표시해 다시 수정할 수 있게 합니다. Maximum Score 경기나 핸디캡 제출용 상한은 원본 퍼팅 수를 덮어쓰지 않는 별도 계산으로 처리합니다.
- 결과: 샷 요약과 GIR·퍼팅 통계가 공의 위치와 클럽 사용을 혼동하지 않으며, 기록 중에는 사용자가 확정한 정보만 표시합니다.

## DEC-025 — 홀 입력 화면의 현재 작업을 단계적으로 강조

- 상태: 확정
- 맥락: 샷 행을 모두 펼치면 빠르게 수정할 수 있지만, 라운드 중에는 다음에 무엇을 입력해야 하는지 찾기 어렵고 완료·미사용 행의 위계가 같아 화면이 복잡해 보입니다.
- 결정: 현재 입력 행은 옅은 초록 배경, 짧게 반복한 뒤 멈추는 초록 점, `지금 입력` 문장으로 강조합니다. 필수값이 채워지면 다음 행으로 안내를 이동하고, 완료 행은 체크, 아직 건드리지 않은 미래 또는 미사용 행은 낮은 명도로 표시합니다. 미래 행도 사용자가 터치하거나 일부 값을 입력하면 딤을 해제하되, 현재 작업 안내는 가장 앞의 미완성 행에 유지합니다. 모션 감소 환경에서는 점 애니메이션을 사용하지 않습니다.
- 결정: 저장 전 의미 있는 값이 남아 있는 홀은 스코어카드에서도 옅은 초록 카드와 홀번호 옆 `● 입력중`으로 표시합니다. 가장 최근에 수정한 홀의 점만 짧게 점멸하며, DB에서 자동으로 채운 PAR·거리만 있는 상태나 화면을 단순히 열어본 경우는 입력 중으로 보지 않습니다.
- 결정: `샷 추가`는 샷 묶음 안의 저강도 텍스트 버튼으로 유지하고, 구분선은 버튼 아래에 두어 그 이후를 퍼팅 영역으로 인식하게 합니다.
- 결과: 입력해야 하는 작업, 이미 완료한 데이터, 지금 입력하지 않아도 되는 데이터의 순서가 명확해지면서 기존 비순차 수정 기능은 유지됩니다.

## DEC-026 — 작성 중 저장과 완료 홀 수정을 분리

- 상태: 확정
- 맥락: 퍼팅 전에도 현장 입력을 보존해야 하지만, 완료된 홀을 다시 열었을 때 빈 기본 샷 행과 항상 활성화된 저장 버튼은 기록 확인과 편집 상태를 혼동하게 합니다.
- 결정: 미완성 홀은 `임시 저장`으로 현재 입력을 보존하고 스코어카드의 `입력중` 상태로 돌아갑니다. 퍼팅까지 계산 가능한 경우에만 `홀 기록 저장`으로 확정합니다.
- 결정: 완료된 홀은 입력된 샷과 퍼팅만 요약해 보여주고, `홀 기록 수정`을 눌러야 전체 편집 화면을 엽니다. 수정 모드에서는 기존 트러블이 있는 샷만 기본으로 펼치고 나머지는 접힌 상태를 유지합니다. 편집 후 실제 변경이 있고 홀 계산 조건이 충족될 때만 `변경사항 저장`을 활성화하며, 저장하지 않고 뒤로가면 기존 확정 기록을 유지합니다.
- 결정: 다음 미완성 샷 강조는 유지하되 퍼팅 영역은 딤 처리하지 않습니다. 사용자가 퍼팅 값을 입력하면 샷 강조를 종료하고 퍼팅 단계로 전환합니다.
- 결과: 라운드 중 중간 저장은 쉬워지고, 완료 기록의 확인·수정·취소 의미가 분리됩니다.

## DEC-027 — 모바일 중단 후 마지막 기록 화면 복원

- 상태: 확정
- 맥락: 라운드 중 화면 잠금, 전화, 카메라 사용이나 Safari 재실행 뒤 홈으로 돌아가면 사용자는 저장 여부를 알 수 없고 기록하던 라운드와 홀을 다시 찾아야 합니다.
- 결정: 마지막 화면이 홀 상세이면 같은 라운드의 같은 홀, 스코어카드이면 같은 라운드의 스코어카드를 복원합니다. 사용자가 앱 내 탐색으로 홈에 돌아간 경우에는 홈을 기억합니다. 대상 라운드나 홀이 사라졌다면 스코어카드 또는 홈으로 안전하게 대체합니다.
- 결정: 작성 중 홀은 값 변경 시 자동 임시 저장하고, `visibilitychange`와 `pagehide` 시점에도 최신 초안을 한 번 더 동기 저장합니다. 복원 시 짧은 안내를 표시합니다.
- 결과: 모바일 사용이 외부 요인으로 중단돼도 기록 위치와 입력값을 다시 찾는 수고를 줄입니다. 전체 메뉴와 백 버튼 계층은 별도 내비게이션 설계에서 재검토합니다.

## DEC-028 — 완료 라운드는 결과 화면을 먼저 표시

- 상태: 임시 확정
- 맥락: 완료 라운드가 곧바로 18홀 스코어카드를 열면 전체 결과와 통계 정책을 검토할 화면이 없고, 사용자가 라운드의 핵심 결과를 빠르게 파악하기 어렵습니다.
- 결정: 완료 기록은 총점과 파 대비를 가장 크게 보여주는 결과 화면을 먼저 엽니다. 전반·후반 합계와 FIR, GIR, 퍼팅, 벌타를 함께 표시하고 기존 스코어카드와 라운드 정보 수정 화면으로 이동할 수 있게 합니다.
- 결정: 현재 FIR·GIR은 성공 수와 적용 가능한 홀 수를 함께 표시하며, 값이 없는 홀은 분모에서 제외합니다. 이 계산 정책은 실제 데이터 검토 후 `TASK-002`에서 최종 확정합니다.
- 결과: 결과 요약과 홀별 기록의 정보 위계가 분리되며, 완료 라운드의 통계 정책을 실제 화면에서 검증할 수 있습니다.

## DEC-029 — 라운드 구조 정보는 초기 3개 홀까지만 변경

- 상태: 확정
- 맥락: 골프장과 전·후반 코스는 공식 홀번호·PAR·거리 및 홀별 기록에 연결되므로 일반 정보처럼 언제든 바꾸면 기존 기록의 의미가 달라집니다. 반면 첫 홀에서는 실제 배정 코스를 잘못 선택했다는 사실을 미처 알아차리지 못할 수 있습니다.
- 결정: 골프장과 전·후반 코스는 실제 플레이 순서의 세 번째 홀까지 변경할 수 있고, 네 번째 플레이 홀에 저장 기록 또는 의미 있는 임시 입력이 생기면 잠급니다. 비순차 입력으로 네 번째 이후 홀을 먼저 기록한 경우에도 잠급니다.
- 결정: 초기 홀 기록이 있는 상태에서 구조 정보를 바꾸면 사용자가 `기록 유지하고 코스만 변경` 또는 `홀 기록 지우고 다시 시작`을 선택합니다. 유지 시 샷·클럽·트러블·퍼팅을 플레이 순서에 유지하면서 새 홀번호·PAR·거리로 다시 연결하고, 다시 시작하면 홀 기록과 초안만 삭제합니다.
- 결정: 완료 라운드는 골프장과 전·후반 코스를 잠그고 날짜·시간·티그라운드·단위·동반자 메모만 수정합니다.
- 결과: 초기 선택 실수는 현장에서 바로잡을 수 있으면서 네 번째 홀 이후에는 자동 재연결로 기존 기록이 잘못 해석되는 위험을 막습니다. 복잡한 완료 기록 교정은 별도 `기록 바로잡기` 흐름으로 후속 검토합니다.

## DEC-030 — 스코어카드 뒤로가기와 정보 수정을 분리

- 상태: 확정
- 맥락: 진행 중 스코어카드의 뒤로가기가 라운드 정보 화면을 열고, 정보 화면의 뒤로가기가 다시 스코어카드를 열어 두 화면만 반복되는 탐색 고리가 생겼습니다.
- 결정: 진행 중 스코어카드의 뒤로가기는 홈의 라운드 목록으로 이동합니다. 라운드 정보 수정은 높이를 확보한 스코어카드 상단의 골프장·티·날짜 정보 박스를 눌러 진입합니다. 별도 `정보 수정` 문구는 추가하지 않습니다. 완료 스코어카드는 기존 결과 화면으로 돌아갑니다.
- 결과: 뒤로가기는 사용자가 예상하는 상위 목록 이동으로 동작하고, 정보 수정은 별도의 명시적인 진입점으로 유지됩니다.

## DEC-031 — 홀 저장과 라운드 완료를 분리

- 상태: 확정
- 맥락: 마지막 홀을 저장하자마자 결과 화면으로 이동하면 사용자가 18홀 스코어카드와 최종 합계를 검토할 기회가 없고, 홀 저장과 전체 라운드 확정의 의미가 섞입니다.
- 결정: 홀 기록 저장은 해당 홀만 확정하고 항상 스코어카드로 돌아갑니다. 18홀 점수가 모두 저장된 경우에만 별도의 `라운드 완료` 버튼을 활성화합니다.
- 결정: 미입력 홀이 있으면 완료 버튼을 비활성화하고 실제 표시 홀번호를 안내합니다. 18홀이 준비되면 최종 확인 시트에서 입력 수, 파 대비와 총 타수를 보여준 뒤 사용자가 명시적으로 완료합니다.
- 결정: 완료 후에도 홀 기록 수정은 허용하며 저장 즉시 결과와 통계를 다시 계산해 결과 화면으로 돌아갑니다.
- 결과: 사용자는 홀 단위 저장과 라운드 전체 확정을 구분하고, 누락 기록과 최종 점수를 확인한 뒤 의도적으로 완료할 수 있습니다.

## DEC-032 — 통계 결측값은 분모에서 제외하고 표본 수를 보존

- 상태: 확정
- 맥락: 미입력 값을 0이나 실패로 계산하면 실제 플레이 성과가 왜곡됩니다. 반대로 총 타수와 파 대비처럼 필요한 원천 데이터가 서로 다른 지표도 같은 분모를 쓰면 안 됩니다.
- 결정: 총 타수는 점수가 기록된 홀을 합산합니다. 파 대비와 스코어 결과 분포는 점수와 PAR가 모두 있는 홀만 계산합니다.
- 결정: FIR은 값이 확정된 파4·파5만, GIR은 GIR 값이 확정된 홀만 분모로 사용합니다. 파3 FIR과 미확정 값은 제외합니다.
- 결정: 공식 퍼팅 수가 있으면 이를 우선하고, 없으면 기존 퍼팅 수를 호환값으로 사용합니다. 둘 다 없으면 퍼팅 합계·평균·분포의 분모에서 제외하고 표본이 전혀 없으면 `—`로 표시합니다.
- 결정: 거리와 클럽의 미입력은 0m나 실패로 바꾸지 않습니다. 향후 거리·클럽 통계에는 실제 표본 수와 기록 완성도를 함께 표시합니다.
- 결과: 부분 입력과 과거 호환 데이터가 있어도 통계가 과장되지 않으며, 각 수치는 실제로 기록된 기회 수와 함께 해석할 수 있습니다.

## DEC-033 — 라운드는 로컬 우선 저장 후 Supabase와 병합

- 상태: 임시 확정
- 맥락: 모바일 현장 기록은 네트워크가 불안정해도 중단되면 안 되지만, 브라우저 저장소만 사용하면 PC·휴대폰 간 기록이 다르고 브라우저 데이터 삭제 시 복구하기 어렵습니다.
- 결정: 화면 입력은 로컬 저장소에 먼저 반영하고, 로그인 사용자와 Supabase 연결이 준비되면 라운드 전체 원본을 서버에 비동기 저장합니다. 앱 시작 시 로컬·서버 양쪽의 고유 기록을 보존하고 같은 ID는 `updatedAt`이 더 최근인 기록을 사용합니다.
- 결정: 서버의 `rounds.payload`를 Alpha의 복구 가능한 원본으로 두고, DB 트리거가 `round_holes`와 `round_shots`를 자동 갱신합니다. RLS는 모든 사용자 데이터 테이블에서 `auth.uid()`와 소유자 ID가 같은 행만 허용합니다.
- 결정: 서버 오류는 로컬 저장을 막지 않고 동기화 지연 상태로 안내합니다. 미완성 홀 초안은 사용자별 로컬 저장소와 `rounds.payload.draftHoles`에 함께 저장하고, 같은 홀의 기기·서버 초안 중 `draftUpdatedAt`이 최근인 값을 복원합니다. 홀 저장 또는 라운드 완료 시 해당 초안을 제거합니다.
- 결정: 브라우저가 오프라인이면 기기 저장을 계속하고 상태를 안내합니다. 연결 복구 또는 일시적인 서버 오류 뒤에는 사용자의 추가 조작 없이 서버 저장을 자동 재시도합니다.
- 결과: 현재 입력 속도와 같은 기기의 즉시 복구를 유지하면서, 저장 전 홀 입력도 동일 계정의 다른 기기에서 이어갈 수 있고 일시적인 통신 장애가 영구적인 동기화 누락으로 남지 않습니다.

## DEC-034 — 로그아웃은 현재 기기 세션만 종료

- 상태: 확정
- 맥락: 같은 사용자가 PC와 모바일에서 동시에 기록할 수 있으므로 한 기기의 로그아웃이 다른 기기의 현장 기록을 중단시키면 안 됩니다.
- 결정: 일반 로그아웃은 Supabase의 로컬 범위로 실행해 현재 브라우저 세션만 종료합니다. 다른 기기의 로그인과 서버 동기화는 유지합니다.
- 결정: 로그아웃 후 Google 로그인을 다시 시작할 때는 브라우저에 남은 Google 세션으로 직전 계정이 자동 선택되지 않도록 계정 선택 화면을 표시합니다.
- 결정: 앱 중단·화면 잠금·새로고침은 마지막 작성 위치를 복원하지만, 사용자가 명시적으로 로그아웃하면 해당 계정의 화면 복귀 위치만 홈으로 초기화합니다. 작성 중 라운드와 홀 임시저장은 유지합니다.
- 결과: 모바일에서 로그아웃한 뒤에도 PC 작업은 계속 서버에 저장되며, 모바일 재로그인 시 같은 계정의 최신 서버 기록을 다시 불러옵니다.

## DEC-035 — 계정 전환 시 화면 상태를 사용자별로 격리

- 상태: 확정
- 맥락: RLS가 서버 행 접근을 분리해도 같은 브라우저에서 계정을 전환할 때 이전 사용자의 React 메모리 상태를 초기화하지 않으면 새 사용자의 로컬·서버 기록과 잘못 병합될 수 있습니다. 또한 새 기기의 기존 사용자는 서버 프로필 확인 전에 온보딩 화면이 잠깐 노출될 수 있습니다.
- 결정: 인증 사용자가 바뀌거나 로그아웃하면 라운드, 활성 라운드, 골프장 이력과 화면 복원 상태를 즉시 비웁니다. 로컬 프로필이 없는 로그인은 서버 프로필 확인이 끝날 때까지 준비 화면을 유지하고, 서버 온보딩 완료 여부에 따라 홈 또는 온보딩으로 분기합니다.
- 결과: 서로 다른 계정의 기록이 클라이언트에서 섞이지 않으며, 새 기기의 기존 사용자는 온보딩을 반복하지 않습니다.

## DEC-036 — 선택 티 거리 결측 안내는 입력 전후 역할을 분리

- 상태: 확정
- 맥락: 골프장 홀 정보가 연결돼도 레드 티 전체 또는 골드 티 일부처럼 선택 티의 공식 거리가 제공되지 않는 경우가 있습니다. 라운드 중에는 사용자가 직접 입력해야 한다는 사실이 중요하고, 완료 후에는 실제 분석에서 빠진 범위를 설명해야 합니다.
- 결정: 선택 티의 공식 거리가 없는 홀은 해당 홀 입력 화면 상단에서 직접 입력을 안내하고 홀거리 입력칸을 강조합니다. 전체 홀이 누락된 티는 골프장 단위 안내 문구를, 일부 홀만 누락된 티는 해당 홀 안내 문구를 사용합니다. 완료 결과는 원본에 없던 거리 중 사용자가 끝까지 입력하지 않은 홀 수만 거리 기반 분석 제외 안내로 표시합니다.
- 결과: 골프장 원본의 결측을 다른 티의 거리나 0으로 추정하지 않으면서, 사용자가 직접 보완한 데이터는 정상 표본으로 활용하고 불필요한 완료 경고를 남기지 않습니다.

## DEC-037 — 클럽 비거리는 시점별 전체 세트로 보존

- 상태: 확정
- 맥락: 클럽 하나의 거리 변화도 중요하지만 실제 플레이에서는 같은 시점에 골프백 전체의 거리 간격을 함께 참고합니다. 클럽별 최신값만 독립적으로 덮어쓰면 어느 시점에 함께 사용하던 구성이었는지 알 수 없습니다.
- 결정: 클럽 구성과 비거리 등록을 두 단계로 나눕니다. 비거리 저장 한 번은 그 날짜의 전체 클럽 세트를 만들며, 사용자가 일부 클럽만 입력하면 같은 단위와 거리 기준의 직전 세트에서 나머지 값을 이어받습니다. 저장 직후에는 이번에 직접 입력한 클럽만 연한 초록 배경으로 표시하고, 화면을 다시 열면 강조를 제거합니다. 별도의 `NEW` 문구는 사용하지 않습니다.
- 결정: 최초 등록은 클럽 구성을 완료해야 비거리 단계로 이동합니다. 구성 완료 이후 `내 골프백`은 비거리 화면으로 바로 진입합니다. `클럽 구성` 탭은 선택한 클럽만 읽기 전용으로 보여주고, `클럽 수정하기`를 눌렀을 때만 전체 선택 목록과 직접 추가 기능을 표시합니다. 기존 구성을 수정·저장한 뒤에는 비거리 화면으로 이동하지 않고 구성 완료 화면으로 돌아와 변경 결과를 먼저 보여줍니다. 저장 이력이 없어도 선택한 모든 클럽과 `—` 값을 4열 표로 먼저 보여줍니다.
- 결정: 신규 사용자의 기본 구성은 드라이버 `D`와 퍼터 `PT`만 선택하고 나머지는 비워둡니다. 골프백의 개인차가 크므로 추천 구성을 임의로 채우지 않으며, 우드 0개·웨지 5개처럼 클럽 개수와 종류를 제한하지 않습니다. 완료 여부는 클럽 수가 아니라 사용자가 현재 구성을 직접 확인·저장했는지로 판단합니다.
- 변경 결정(2026-08-31): 온보딩은 환영, 기본 티·거리 단위, 클럽 구성의 3단계로 끝냅니다. 클럽 비거리 화면은 온보딩에서 제외하고, 비거리 기록이 없는 사용자가 새 라운드를 시작할 때 선택 기능이 있음을 짧게 안내합니다. 클럽 구성을 확인하지 않은 계정이 새 라운드를 시작하면 클럽 등록 안내 후 골프백으로 이동합니다.
- 결정: 거리 기준은 `캐리`, `총거리`, `미지정`을 허용하고 M/YD 단위를 함께 저장합니다. 단위가 바뀌면 동일 기준의 직전 값을 환산해 보여주되 정규화 미터 값을 보존하고, 기준이 달라지면 이전 값을 자동으로 섞지 않습니다. 빈 값은 `null`로 유지하며 퍼터는 골프백 구성에는 포함하지만 비거리 세트에서는 제외합니다.
- 결정: 거리 기준은 현재 사용자의 기록 편의를 위한 메타데이터이며 Alpha 통계 계산에는 반영하지 않습니다. 분석 활용 방식은 실제 기록이 쌓인 뒤 별도로 결정합니다.
- 결정: 드라이버는 `D`, 우드·유틸리티·아이언은 `3W`·`3UT`·`7I`, 웨지는 사용자가 선택하거나 입력한 `P`·`A`·`S`·`48` 등의 짧은 표시명을 라운드 기록에도 사용합니다.
- 결정: 선택·재추가 순서가 표를 바꾸지 않도록 드라이버, 우드 번호순, 유틸리티 번호순, 아이언 번호순, 웨지 이름·로프트순으로 고정합니다. 4열 표는 왼쪽 열을 먼저 채운 다음 오른쪽 열로 이어집니다.
- 결정: MVP에서는 최신 비거리 세트만 화면에 표시합니다. 과거 세트 데이터는 보존하지만 조회 UI와 달력 아이콘은 제공하지 않습니다. 추후 필요성이 확인되면 달력에서 최근 약 10개 세트를 선택하는 방식으로 검토합니다.
- 보존 원칙: 사용자가 저장한 클럽 비거리 세트는 새 입력으로 덮어쓰거나 임의 삭제하지 않고 작성 시점별 원본 이력으로 계속 보존합니다. MVP에서 과거 이력을 화면에 노출하지 않는 것은 조회 기능을 보류한 것이며 데이터 삭제를 뜻하지 않습니다.
- 결정: 비거리 단위 변경은 기존 값을 삭제하지 않고 `1m = 1.0936133yd`로 환산하며, 반복 전환의 반올림 오차를 막기 위해 정규화한 미터 값도 함께 보존합니다. 비거리 기준은 `미지정 / 캐리 / 총거리` 세 상태로 저장하고, 기준이 바뀌면 다른 기준의 값을 자동 승계하지 않습니다. 비거리 화면의 단위 변경은 온보딩 기본 단위를 바꾸지 않습니다.
- 결과: 한 시점의 클럽 간 거리 체계를 재현하면서 일부 클럽만 간편하게 갱신할 수 있고, 장기간의 골프백 구성과 거리 변화를 날짜별로 비교할 수 있습니다.

## DEC-038 — 클럽 구성과 비거리 이력은 계정 단위로 병합·보존

- 맥락: 클럽 구성과 비거리 세트가 브라우저 로컬 저장소에만 있으면 PC와 모바일에서 진입 화면과 값이 달라지고, 기기를 바꾸거나 저장소를 비우면 과거 이력을 잃을 수 있습니다.
- 결정: 로그인 사용자의 클럽 구성과 모든 비거리 세트를 Supabase에 비동기 저장합니다. 클럽은 앱의 고유 ID로 연결하고, 구성에서 빠진 클럽도 과거 세트 참조를 위해 비활성 상태로 보존합니다.
- 병합 규칙: 클럽 구성은 `updatedAt`이 더 최근인 쪽을 사용합니다. 서로 다른 비거리 세트 ID는 로컬·서버 양쪽을 모두 유지하고, 같은 세트 ID는 작성 시각이 더 최근인 값을 사용합니다.
- 보존 규칙: 거리값이 없는 클럽도 해당 시점의 전체 세트 구성원으로 저장하며, 거리 기준·단위·정규화 미터 값·당시 클럽 표시명을 함께 보존합니다.
- 결과: 같은 계정으로 다시 로그인하거나 다른 기기에서 열어도 현재 골프백과 최신 비거리 화면이 복원되고, 향후 이력 조회 UI를 추가할 때 과거 세트를 재수집할 필요가 없습니다.

## DEC-039 — 홈 누적 통계는 완료 기록의 유효 표본을 합산

- 상태: 확정
- 맥락: 라운드마다 FIR·GIR·퍼팅의 기록 수가 다를 수 있으므로 라운드별 퍼센트를 다시 평균하면 표본이 적은 라운드가 과도한 영향을 줍니다.
- 결정: 홈 리포트는 `completed` 상태의 라운드만 집계합니다. 평균 스코어와 베스트 스코어는 점수가 있는 완료 라운드, FIR·GIR·평균 퍼팅은 모든 완료 라운드의 성공 수·유효 기회 수·퍼팅 합계를 먼저 더한 뒤 계산합니다.
- 결정: 홈에는 평균 스코어, 총 라운드, GIR 평균, 평균 퍼팅, FIR 평균, 베스트 스코어를 3×2로 표시합니다. 완료 기록이 없으면 0을 성과처럼 표시하지 않고 라운드를 완료하면 통계가 생성된다는 빈 상태를 제공합니다.
- 결과: 부분 입력이나 과거 결측 기록이 섞여도 누적 비율이 왜곡되지 않고, 단일 라운드 결과와 동일한 결측값 정책을 유지합니다.

## DEC-040 — 날씨·감정·이메일은 Alpha 기록 흐름에서 제외

- 상태: 확정
- 맥락: 현재 Alpha의 검증 목표는 현장에서 홀 기록을 잃지 않고 완료해 즉시 결과와 누적 통계를 확인하는 것입니다. 날씨와 감정은 아직 결과 계산에 쓰이지 않고, 이메일도 즉시 확인 가능한 앱 결과를 반복 전달할 명확한 이유가 없습니다.
- 결정: 날씨는 위치 권한, API 실패, 과거 라운드 날씨 정합성과 날씨별 분석을 함께 설계하는 Phase 2로 이동합니다. 현재 라운드 입력에는 권한 요청이나 수동 날씨 필드를 추가하지 않습니다.
- 결정: 감정 체크는 코칭 분석에서 실제 사용 규칙이 생길 때 도입합니다. 현재는 매 홀·라운드 입력 부담을 줄이기 위해 제외합니다.
- 결정: 이메일은 주간·월간 리포트처럼 앱을 다시 열 이유가 명확해질 때 발송 조건, 사전 동의, 수신 해지, 실패 처리를 함께 구현합니다. Alpha에서는 앱 내 결과·저장·복구 안내를 우선하며 외부 발송 자격증명을 요구하지 않습니다.
- 결과: 핵심 기록과 복구 품질에 집중해 Alpha를 마무리하고, 사용 목적이 없는 입력·알림과 외부 서비스 운영 부담을 만들지 않습니다.

## DEC-041 — Alpha 품질 검증은 실제 기기 확인과 자동 회귀를 함께 사용

- 상태: 확정
- 결정: 인증·온보딩·계정 간 격리·PC/모바일 동기화는 실제 Google 계정과 기기에서 확인합니다. 기록 계산·결측 정책·저장 병합·클럽 이력·접근성 구조는 자동 회귀 테스트로 고정합니다.
- 결정: 프리뷰 데이터로 홈, 완료 결과, 스코어카드, 홀 상세와 누적 통계의 연결을 반복 검증합니다. 모달은 열린 뒤 조작 가능한 요소에 초점을 주고 Escape 닫기와 이전 초점 복귀를 제공합니다.
- 결과: 매 변경마다 모든 실제 기기 절차를 반복하지 않아도 핵심 로직 회귀를 빠르게 발견하면서, 브라우저·OAuth처럼 자동 테스트만으로 부족한 구간은 실제 환경 증거를 유지합니다.

## DEC-042 — React·Vite·CSS와 OpenAI Sites로 HTTPS Alpha 임시 운영

- 상태: DEC-044로 대체
- 맥락: 실제 구현은 Tailwind가 아니라 React·Vite·일반 CSS로 구성됐고, 로컬 네트워크 주소는 현장 사용·OAuth·기기 간 검증에 적합하지 않습니다.
- 결정: 프론트엔드 기술 기준은 실제 구현에 맞춰 React + Vite + CSS로 바로잡습니다. OpenAI Sites의 `https://golf-and-me.kimseungha.chatgpt.site` 배포는 현재 Alpha 기능 검증에 사용하는 임시 주소로 유지합니다.
- 보정: OpenAI Sites를 장기 호스팅 정책으로 확정한 것은 사용자의 명시적 의사결정 없이 기존 Vercel 방향을 변경한 것이므로 유효한 영구 결정으로 보지 않습니다. Vercel 유지, OpenAI Sites 유지 또는 다른 호스팅 선택은 비교 검토 후 사용자가 새 결정을 시작·승인할 때 확정합니다.
- 결정: 사용자가 한 번 온라인으로 연 프로덕션 앱의 정적 앱 셸은 서비스 워커로 기기에 저장합니다. 이후 일시적인 오프라인 재진입·새로고침에서는 앱을 열고 로컬 우선 입력을 계속하며, 서버 저장은 연결 복구 뒤 자동 재시도합니다.
- 결정: 배포 전 자동 테스트·빌드와 프로덕션 기본 동작을 확인하고, 실제 iPhone Safari의 18홀 전체 흐름은 별도 릴리스 점검으로 유지합니다.
- 결과: 현재 Alpha 주소와 앱 셸은 중단하지 않되, 호스팅 이전·고정 여부·도메인 구성에는 추가 변경을 하지 않고 사용자 결정을 기다립니다.

## DEC-043 — 초기 정책 변경은 사용자 명시 결정 후 실행

- 상태: 확정
- 맥락: 구현을 빠르게 진행하라는 요청과 제품의 초기 정책 자체를 바꾸라는 승인은 서로 다릅니다. 호스팅처럼 되돌릴 수 있는 변경도 인증·도메인·배포 절차와 향후 운영 비용에 영향을 주므로 구현자가 묵시적으로 결정해서는 안 됩니다.
- 결정: 초기 제품·기술·운영 정책의 변경은 사용자가 문제를 인지하고 명시적으로 결정한 뒤에만 실행합니다. 구현자가 이슈를 발견하면 충분히 검토한 뒤 `기존 정책 변경이 필요한 사안`임을 먼저 밝히고, 현재 정책·근거·대안·영향·복구 범위를 제시합니다.
- 결정: `알아서 진행` 같은 포괄적 위임은 확정 정책 안의 구현과 검증에만 적용합니다. 호스팅, 저장소와 배포 경로, DB, 인증, 도메인, 데이터 보존·삭제, 결제, 외부 사업자, 핵심 아키텍처는 별도 명시 승인이 필요합니다.
- 결과: 명시적 결정 전에는 기존 정책과 외부 상태를 보존합니다. 임시 검증은 영구 결정으로 승격하지 않으며, 결정 문서는 사용자 승인 뒤에만 확정 상태로 갱신합니다.

## DEC-044 — 정식 배포 경로를 Vercel로 복구

- 상태: 확정
- 대체: `DEC-042`의 OpenAI Sites 임시 운영안을 정식 배포 경로로 사용하지 않습니다.
- 맥락: 초기부터 Git 저장소와 Vercel을 정식 배포 경로로 사용하기로 검토했으며, 사용자가 해당 방향으로 즉시 복구할 것을 명시적으로 결정했습니다.
- 결정: GitHub `main` 브랜치를 Vercel Production에 연결하고 Vite 정적 빌드를 배포합니다. Supabase와 Google OAuth의 허용 주소는 검증된 Vercel Production 주소를 기준으로 갱신합니다.
- 결정: 기존 OpenAI Sites 배포는 Vercel 로그인·저장·모바일 검증이 끝날 때까지 롤백 참고용으로 유지하며, 삭제는 별도 사용자 결정 없이 실행하지 않습니다.
- 결과: `https://golf-and-me.vercel.app`에서 Google OAuth 로그인과 기존 계정의 작성 중·완료 기록 복원을 확인했습니다. 이후 정식 배포와 운영 문서는 Vercel을 기준으로 하며, 호스팅 제공자를 다시 변경하려면 `DEC-043`의 승인 절차를 따릅니다.

## DEC-045 — OpenAI Sites 레거시 배포와 인증 주소 종료

- 상태: 확정
- 맥락: Vercel Production에서 Google OAuth 로그인, 기존 계정 데이터 복원, PC·모바일 접근을 검증해 이전 Alpha 주소를 롤백 경로로 유지할 필요가 없어졌습니다.
- 결정: 사용자 명시 승인에 따라 OpenAI Sites 프로젝트의 공개 접근을 종료하고 소유자 전용으로 전환합니다. 현재 Sites 관리 API는 프로젝트 영구 삭제를 제공하지 않으므로 외부 방문자가 사용할 수 없는 상태를 삭제에 준하는 운영 종료로 봅니다.
- 결정: Supabase Auth Redirect URL 허용 목록에서 `https://golf-and-me.kimseungha.chatgpt.site/**`를 제거하고, Vercel Production 주소와 로컬 개발 주소만 유지합니다.
- 결과: 정식 배포·인증·롤백 기준은 Vercel로 단일화합니다. 코드와 배포 이력은 Git에 보존하며, 호스팅 제공자를 다시 변경할 때는 DEC-043에 따라 별도 승인을 받습니다.

## DEC-046 — 계정 탈퇴 시 본인 계정과 전체 기록을 영구 삭제

- 상태: 확정
- 맥락: 공개 Alpha에서도 사용자가 서비스 안에서 계정과 개인 기록을 직접 삭제할 수 있는 경로가 필요합니다.
- 결정: 계정 메뉴에 별도 확인 화면을 두고, 복구할 수 없다는 범위와 영향을 명확히 안내한 뒤에만 삭제합니다. Supabase 함수는 인증된 현재 사용자 ID만 삭제하며 공개 실행 권한을 허용하지 않습니다.
- 결정: 서버 계정 삭제의 외래키 연쇄 삭제로 프로필·라운드·홀·샷·클럽·비거리 이력을 함께 제거하고, 성공 후 현재 사용자의 Golf & Me 로컬 저장 키만 정리합니다. 다른 계정과 무관한 브라우저 데이터는 건드리지 않습니다.
- 결과: 실제 사용자 계정은 자동 회귀시험에서 삭제하지 않으며 전용 테스트 계정으로만 최종 수동 검증합니다. 운영자·문의처·국외 이전·로그 보관 정보와 최종 정책 게시가 끝날 때까지 TASK-032는 진행 중으로 유지합니다.

## DEC-047 — Free Alpha는 수동 DB 백업과 복구 모의훈련을 사용

- 상태: 확정
- 맥락: 현재 Supabase Free 플랜은 Pro 이상처럼 자동 일일 백업을 제공하지 않으므로 공급자의 복구 지점이 항상 있다고 가정할 수 없습니다.
- 결정: 공개 Alpha에서는 스키마 변경·마이그레이션·대량 수정 전과 주 1회에 Supabase CLI로 역할, 스키마, 데이터를 한 세트로 내보냅니다. 백업은 사용자 데이터가 포함된 비공개 운영 자료로 취급하고 Git 저장소에는 넣지 않습니다.
- 결정: 백업 생성만으로 완료로 보지 않습니다. 새 테스트 프로젝트에 복원해 핵심 테이블, 로그인, RLS와 최근 기록을 확인한 뒤에만 복구 검증 완료로 표시합니다. 장애 중 운영 DB에는 즉흥 수정이나 자동 롤백을 하지 않고 현재 상태 보존과 원인 확인을 먼저 합니다.
- 결정: 사용자 수와 기록 가치가 커지기 전에 Pro 자동 일일 백업 또는 PITR 도입을 별도로 결정합니다. Storage 파일 기능을 추가하면 DB와 별개로 파일 객체 백업도 설계합니다.
- 결과: `TASK-034`는 정책 확정 상태에서 시작하지만, 실제 CLI 백업과 새 프로젝트 복원 모의훈련이 끝날 때까지 진행 중으로 유지합니다.

## DEC-048 — 퍼팅 입력과 홀 완료를 분리

- 상태: 확정
- 맥락: 퍼팅 수를 먼저 입력한 뒤 실제 후속 샷을 보완해야 하는 상황에서도 화면이 완료 가능한 상태로 바뀌어 누락된 샷을 확정할 위험이 있습니다.
- 결정: 퍼팅 선택은 스코어 미리보기만 갱신합니다. 신규 홀에는 `임시 저장`과 `홀 기록 완료`를 함께 제공하고 사용자가 완료 버튼을 누른 경우에만 완료 상태로 전환합니다.
- 결정: 완료 전 `샷 · 퍼팅 · 벌타 = 총타수`를 항상 보여줍니다. 티샷 누락, 사용 샷 사이의 공백, 사용 샷의 클럽 누락, 미결정 OB, 퍼팅 미선택처럼 구조적으로 불완전한 기록만 완료를 막습니다. 거리 누락은 분석 제외를 안내하되 완료를 허용하고, 홀인원·이글·알바트로스처럼 드문 결과는 경고하지 않습니다.
- 결과: 현장에서 급히 일부만 기록해도 안전하게 보존하고, 사용자가 홀 종료를 명시적으로 판단할 수 있습니다.

## DEC-049 — 라운드 클럽은 당시 ID와 표시명 스냅샷으로 보존

- 상태: 확정
- 맥락: 골프백에서 클럽을 제거하거나 같은 표시명으로 다시 추가해도 과거 기록·통계·수정 화면은 당시 선택을 잃거나 현재 클럽으로 바뀌면 안 됩니다.
- 결정: 각 샷은 현재 클럽 ID와 선택 당시 표시명·분류 스냅샷을 함께 저장합니다. 제거된 클럽은 비활성 상태로 보존하고 과거 홀 수정에서는 `과거 사용 클럽`으로 선택지를 유지합니다. 기존 표시명만 있는 기록은 호환하되 새 저장부터 안정 ID를 사용합니다.
- 결정: 같은 기본 클럽을 해제·재선택하거나 같은 분류·표시명의 사용자 정의 클럽을 다시 추가하면 기존 비활성 클럽 ID를 재사용합니다. 구성 수정 횟수는 제한하지 않고 비거리 세트와 과거 샷 스냅샷도 삭제하지 않습니다.
- 결과: 골프백 변경은 이후 입력 목록에만 영향을 주며 과거 스코어카드와 분석 원본은 불변입니다.

## DEC-050 — 최초 라운드 전 실제 클럽 구성을 필수 확인

- 상태: 확정
- 맥락: 클럽 구성이 없는 사용자를 임의 기본 목록으로 기록하게 하면 초기 라운드의 클럽 데이터가 실제 골프백과 달라지고 이후 수정·분석에서 의미를 잃습니다.
- 결정: 신규 사용자는 기본 티와 거리 단위 다음에 실제 클럽 구성을 한 번 확인해야 첫 라운드를 기록할 수 있습니다. 기존 사용자는 첫 라운드 진입 전 한 번만 보완하며, 샷별 거리·클럽 입력 자체는 현장 속도를 위해 선택으로 유지합니다.
- 결과: 클럽 분석 기능을 나중에 제공하더라도 Alpha부터 수집한 식별 가능한 기록을 사용할 수 있습니다.

## DEC-051 — Beta 행동 분석은 비식별 허용 이벤트로 준비

- 상태: DEC-060으로 대체
- 맥락: 로그인과 라운드 기록 중단 지점, 생성부터 완료까지 걸린 시간을 Beta에서 확인해야 하지만 골프 기록 원본과 계정 정보는 분석 도구에 보낼 필요가 없습니다.
- 결정: 로그인 시작·성공·실패, 온보딩 단계, 클럽 구성 완료, 라운드 생성, 홀 임시 저장·완료, 1·3·9·18홀 이정표, 라운드 완료, 결과 열람, 저장 지연·복구, 탈퇴 완료만 허용 이벤트로 정의합니다. 이메일·Google 정보·골프장명·메모·샷 거리·클럽명·원본 라운드 ID는 전송하지 않습니다.
- 결정: 코드는 GTM 없이 GA4 측정 ID가 없으면 동작하지 않는 준비 상태로 구현합니다. 실제 활성화는 사용자가 GA4 계정을 설정하고 동의 문구·개인정보 처리방침·보관 정책을 확정한 뒤 진행합니다.
- 결과: 기능 흐름을 정량 검증할 기반은 미리 갖추되 정책이 끝나기 전에 방문자 추적을 시작하지 않습니다. GA4는 `VITE_ANALYTICS_ENABLED=true`, 유효한 측정 ID, 사용자 동의가 모두 있을 때만 직접 로드됩니다.

## DEC-052 — Beta 요청과 의견은 Slack 접수·전체 새소식으로 가볍게 운영

- 상태: 확정
- 맥락: 지인 중심 Beta에서는 테스트 계정과 골프장 홀 정보 요청을 메신저로 받더라도 놓치지 않아야 하지만, 개인 요청함·처리상태·이메일 회신 같은 운영 기능을 따로 만드는 것은 한시적 목적에 비해 부담이 큽니다.
- 결정: 테스트 계정 신청, 골프장 홀 정보 요청과 의견 보내기는 최소 입력 폼에서 Vercel 서버를 거쳐 비공개 Slack 채널로 전달합니다. Slack을 임시 운영 대기열로 사용하고 개인 상태 화면·자동 이메일·스크린샷 첨부는 만들지 않습니다. 테스트 계정은 로그인 화면에서 이메일만, 홀 정보 요청은 미연동 골프장의 새 라운드 안내에서 골프장명과 선택 URL만, 의견 보내기는 계정 메뉴에서 로그인 회원의 500자 이내 자유 텍스트 한 칸만 받습니다. 의견 API는 Supabase 세션을 확인하되 Slack에는 이메일·UUID·원본 기록·기기 정보를 보내지 않습니다.
- 결정: 홀 정보 추가·기능 추가·오류 수정은 개인 알림 대신 전체 `새소식`으로 짧고 캐주얼하게 안내합니다. 홀 정보 요청에는 3~4영업일 처리 목표와 공식 정보 확인 또는 제공 조건에 따라 반영되지 않을 수 있다는 안내를 함께 표시합니다.
- 결정: 우선순위는 테스트 계정 신청과 전체 새소식을 먼저 구현하고, 골프장 홀 정보 요청과 의견 보내기는 출시 차단 작업과 로그인 성능 점검을 마친 뒤 천천히 착수합니다. 야디지 자동 수집·DB 자동 게시와 개인 알림은 Beta 요청량을 확인한 뒤 별도로 결정합니다.
- 결정: 테스트 계정 신청은 GCP OAuth 정식 운영 승인 전까지만 사용합니다. 승인이 완료되어 테스트 사용자 허용 목록이 불필요해지는 즉시 로그인 화면의 신청 UI, Vercel 신청 API와 Slack Webhook 환경변수를 함께 제거합니다.
- 결과: Beta 사용자와의 소통 창구를 확보하면서도 한시 운영을 위한 관리자·개인화 기능을 만들지 않습니다. Slack Webhook 비밀값과 접수 개인정보의 목적·보관 범위는 공개 전 운영정책에 포함합니다.

## DEC-053 — 로그인 속도는 현재 통과 후 GA 실사용 추세로 재검증

- 상태: DEC-060으로 일부 대체
- 맥락: 로그인 데이터 조회 병렬화, WOFF2 서체 축소와 외부 인증 서비스 사전 연결을 적용했습니다. 2026-08-31 실제 모바일 녹화 2건에서는 Google 계정 화면을 벗어난 뒤 사용 가능한 Golf & Me 화면까지 약 0.1~0.5초로 확인됐지만, 서로 다른 계정·캐시 상태라 전후 성능을 엄밀하게 비교하기에는 표본이 부족합니다.
- 결정: 현재 로그인 속도 이슈는 MVP 진행을 막지 않는 것으로 통과 처리합니다. Google 계정 선택창 자체의 지연과 사용자가 계정을 선택하기까지의 시간은 앱 성능과 분리합니다.
- 결정: GA4 직접 연동 활성화 후 동의 사용자에 한해 로그인 시작을 실제 이벤트 `auth_attempt`(`stage=oauth_request`)로, 로그인 성공을 `login_success`의 `session_restored`·`records_ready`와 `duration_ms`로 수집해 기기·브라우저별 추세를 확인합니다. 계정 정보와 골프 기록 원본은 분석 도구에 전송하지 않습니다. 기존 `login_start`는 실제 Preview에서 수신되지 않아 운영 이벤트명으로 사용하지 않습니다.
- 결과: 지금은 추가적인 체감 최적화보다 MVP 필수 기능에 집중하고, 실제 Beta 데이터에서 반복적인 지연이 확인될 때만 성능 작업을 다시 엽니다.

## DEC-054 — 사용 규칙은 중앙 안내와 상황별 짧은 도움말로 제공

- 상태: 확정
- 맥락: 라운드 생성, 임시 저장과 홀 완료, 벌타 계산, 이어쓰기, 구조 변경과 클럽 이력처럼 사용자가 알아야 할 규칙이 늘었지만 긴 설명서를 온보딩에서 강제하면 첫 사용 부담이 커집니다.
- 결정: 계정 메뉴의 `사용 안내`를 변하지 않는 이용 규칙의 기준 화면으로 둡니다. 내용은 `라운드 시작하기`, `홀 기록하기`, `벌타 기록하기`, `저장과 이어쓰기`, `기록 수정과 골프백`의 5개 접힌 항목으로 구성합니다. 전체 새소식은 서비스 변경 알림 역할만 유지합니다.
- 결정: 등록 라운드가 없는 첫 홈에는 `처음 기록하시나요? 사용 안내 보기`라는 작은 링크를 표시하고 첫 기록이 생기면 숨깁니다. 실수 가능성이 큰 라운드 생성, 홀 완료, 저장 지연, 홀거리 미제공, 최초 클럽 미확인 상황에는 관련 규칙만 짧게 다시 안내하되 모든 화면에 반복 노출하지 않습니다.
- 결과: 사용자는 필요할 때 전체 규칙을 다시 찾을 수 있고, 현장 기록 화면은 긴 설명 없이 가볍게 유지됩니다.

## DEC-055 — 브라우저 OAuth는 PKCE를 사용하고 콜백 정보를 즉시 제거

- 상태: 확정
- 맥락: Supabase JavaScript의 기본 implicit 흐름은 Google 로그인 후 접근·갱신 토큰을 URL fragment에 포함하므로 주소창 복사, 화면 공유와 브라우저 기록에서 인증정보가 노출될 수 있습니다.
- 결정: 웹 Google 로그인은 `flowType: pkce`와 URL 세션 감지를 사용합니다. 새 로그인은 짧은 수명의 일회용 코드만 받고 같은 브라우저의 verifier로 세션을 교환합니다. 코드·흐름 ID·OAuth 오류 파라미터는 교환 후 주소에서 제거합니다.
- 결정: 전환 전에 시작된 implicit 콜백과 이미 열린 주소를 위해 세션 초기화가 끝난 직후 접근 토큰, 갱신 토큰, Google provider token과 관련 만료 정보가 포함된 fragment 전체를 `history.replaceState`로 제거합니다. 인증과 무관한 쿼리와 해시는 보존합니다.
- 결과: 신규 로그인 URL에는 장기 세션 토큰이 나타나지 않으며 기존 방식의 민감한 주소도 앱 로딩 후 남지 않습니다. 운영 반영 후 기존 노출 세션을 로그아웃하고 PKCE로 다시 로그인해 회전합니다.

## DEC-056 — 새소식은 기기별 최신 글 확인 점만 제공

- 상태: 확정
- 맥락: 전체 새소식은 가벼운 공지 기능이므로 개인 알림함이나 서버 읽음 동기화는 과하지만 새 글이 추가됐다는 발견성은 필요합니다.
- 결정: `새소식` 앞에는 개인 알림을 뜻하는 종 대신 전체 공지를 뜻하는 선형 확성기 아이콘을 사용합니다. 해당 사용자와 기기의 마지막 확인 최신 글 ID보다 새 글이 있으면 텍스트 우상단에 작고 정적인 점을 표시하고, 새소식 화면을 열면 현재 최신 글까지 확인한 것으로 로컬에 저장합니다. PC와 모바일의 확인 상태는 동기화하지 않으며 숫자 배지·깜빡임은 사용하지 않습니다.
- 결정: 글을 수정하거나 과거 글을 삭제한 경우에는 새 점을 만들지 않고 목록 최상단에 새 고유 ID의 글을 추가할 때만 다시 표시합니다. 카드는 본문 전체를 보여주며 `더보기`나 강제 말줄임을 두지 않습니다. 운영상 제목은 약 30자, 본문은 약 120자 이내를 권장하고 더 긴 설명은 사용 안내나 별도 화면으로 분리합니다.
- 결과: 새소식은 전체 공지라는 성격을 유지하면서도 새 글을 놓치지 않고, 서버 읽음 상태를 추가하지 않아 Beta 운영 구조를 가볍게 유지합니다.

## DEC-057 — Beta 의견은 Slack으로 가볍게 받고 정식 운영 전 답변 가능한 의견함으로 전환

- 상태: 확정
- 맥락: 현재 의견 본문은 Supabase나 기기에 저장하지 않고 비공개 Slack `feedback` 채널에만 남습니다. Beta 운영에는 가볍고 충분하지만 Slack 메시지가 삭제되면 원본을 복구할 수 없고, 회원이 접수 내용·처리 상태·답변을 다시 확인할 수도 없습니다.
- 결정: 현재 필드 테스트 동안에는 Slack 단독 접수를 유지합니다. MVP 종료 전에는 의견을 Supabase에 저장하고 계정 메뉴에서 본인 의견과 운영 답변을 확인할 수 있는 정식 의견함으로 전환합니다. Slack은 운영 알림 수단으로만 사용하며 이메일·이름·회원 UUID를 보내지 않고 의견 ID와 본문만 전달합니다.
- 결정: 구현 전 의견 상태, 답변 권한, RLS, 보관기간, 탈퇴 시 삭제 또는 비식별 처리, 답변 도착 안내 방식을 개인정보처리방침과 함께 확정합니다. 개인화 알림·자동 이메일·복잡한 관리자 화면은 실제 Beta 요청량을 본 뒤 최소 범위로 결정합니다.
- 결과: 지금의 빠른 Beta 운영을 방해하지 않으면서도 정식 운영에서는 의견 유실 방지와 회원 답변 경험을 제공할 수 있습니다.

## DEC-058 — 원격 조회 실패 원인은 분리 측정 후 확장성 구조를 결정

- 상태: 확정
- 맥락: 2026-08-31 PKCE 재로그인 검증 중 원격 프로필·라운드·클럽 기록 조회가 한 번 실패했습니다. 조회 실패 뒤 5초 자동 재시도로 복구 안전망은 보완했지만, 단일 사례만으로 데이터 양·네트워크·인증 세션·Supabase 응답 중 어느 것이 원인인지 확정할 수 없습니다. 다만 라운드와 비거리 이력은 계속 증가하므로 로그인 직후 전체 원본을 불러오는 구조는 장기적인 성능 위험 후보입니다.
- 결정: 관찰 사례의 원인은 추정으로 고정하지 않고 프로필, 라운드, 클럽/비거리 조회별 오류 분류·전송량·소요 시간을 비식별 방식으로 분리 확인합니다. 네트워크·인증·서비스 응답 문제는 사용자 입력을 막지 않는 자동 복구로 다루고, 구조 문제는 확인된 근거를 바탕으로 개선합니다.
- 결정: 초기 홈은 필요한 요약과 최근 기록을 먼저 준비하고, 과거 완료 라운드와 오래된 거리 이력은 페이지·시점 단위로 나누어 불러오는 방안을 검토합니다. 전환 전에는 다른 기기 병합, 작성 중 초안, 오프라인 복구, 누적 통계의 정합성을 함께 검증합니다.
- 결과: 일시 장애를 데이터 구조 탓으로 오판하지 않으면서도, 기록 증가에 따른 구조적 지연을 초기부터 측정·대응 대상으로 관리합니다.

## DEC-059 — 오류 관측은 MVP 최소 범위 후 풀스펙으로 확장

- 상태: DEC-060으로 대체
- 맥락: 현재 화면 안내와 선택적 GA 이벤트만으로는 원격 조회 실패의 원인과 재시도 결과를 운영자가 나중에 확인하기 어렵습니다. 반대로 상세 오류·사용자 식별자·원본 기록을 무분별하게 수집하면 Alpha의 개인정보 원칙을 해칩니다.
- 결정: MVP 오류 관측은 `oauth`, `profile_load`, `rounds_load`, `club_bag_load`, `rounds_save`, `club_bag_save`, `account_delete` 7개 단계와 비식별 오류 분류, 재시도 성공/실패, 소요 시간과 앱 버전으로 제한합니다. 같은 단계·분류의 반복 오류는 최신 발생 시각과 누적 횟수만 갱신해 전송량을 제한합니다. 사용자 ID·이메일·Google 정보·골프 기록·원본 오류 메시지·브라우저 식별 정보는 수집하지 않습니다. 활성화 전 동의, 보관기간, 운영 열람 방법을 확정합니다.
- 결정: 풀스펙 오류 관측은 실제 사용량과 장애 패턴이 확인된 뒤 상세 원인 추적, 분산 추적, 알림, 대시보드, 샘플링, 보관·삭제 관리와 함께 별도 작업으로 도입합니다.
- 결과: MVP에서는 원인 분류와 복구 여부를 판단할 최소 근거를 확보하고, 과도한 추적이나 운영 부담은 실제 필요가 확인된 뒤 확장합니다.

## DEC-060 — GA4 제품 분석과 Supabase 운영 진단 분리

- 상태: 확정
- 맥락: 가입과 라운드 기록의 이탈·완료 시간은 선택 동의를 바탕으로 제품 분석에 쓰고, 앱·DB 저장 실패는 사용자가 GA4 분석을 거부해도 운영상 최소 범위에서 원인을 확인할 필요가 있습니다. 기존의 단일 GA4 진단 전송은 두 목적과 동의 기준을 섞고, GTM·Firebase를 추가하면 현재 React·Supabase MVP에 불필요한 운영층이 늘어납니다.
- 결정: GTM·Firebase·Crashlytics·Sentry는 도입하지 않습니다. 제품 분석은 앱 코드의 직접 `gtag.js` GA4 연동으로만 수행하고, 기본값은 OFF입니다. 사용자가 허용하기 전에는 GA 스크립트를 로드하지 않으며, 철회하면 이후 제품 이벤트 전송을 중단합니다. GA4에는 정의된 화면·퍼널·소요 시간 이벤트와 매개변수만 보내고, 광고 기능·Google Signals·리마케팅·광고 개인화는 사용하지 않습니다.
- 결정: 앱이 직접 전송하는 제품 이벤트는 코드의 17개 허용 목록으로 제한합니다. 사용자가 분석을 허용한 경우 GA4가 기본 측정을 위해 자동 생성하는 `first_visit`, `session_start`, `user_engagement`는 제품 이벤트와 구분한 기술 이벤트로 허용하되, 동의 전·철회 후에는 전송하지 않고 광고 개인화에는 사용하지 않습니다.
- 결정: 서비스 운영의 최소 오류 진단은 GA4와 별도로 Supabase `app_diagnostics`와 인증된 Vercel API를 사용합니다. 브라우저 직접 DB 삽입은 허용하지 않고, 난수 incident ID·허용 실패 단계·정규화 분류·안전한 HTTP 상태·앱 버전·일반화된 플랫폼·온라인 여부·발생/복구 시각과 횟수만 저장합니다. 사용자 UUID는 API 인증에서만 일시 확인하고 DB·로그에 저장하지 않습니다. 이메일·토큰·전체 URL·골프 기록·자유 입력·원본 오류·stack은 양쪽 시스템에 보내지 않습니다.
- 보관: 운영 진단은 마지막 발생 뒤 30일, 복구된 incident는 복구 뒤 7일을 기본 삭제 기준으로 둡니다. 실제 정기 삭제 실행은 Preview 검증과 운영 권한·점검 책임 확정 뒤 활성화합니다.
- 실행 상태(2026-09-01): Production Supabase migration·RLS·server-role 전용 함수와 Vercel Production Secret·앱 배포를 완료했습니다. 비식별 test incident의 실패 2회가 한 행의 count 2로 합쳐지고 복구가 연결되는 DB 함수 검증 뒤 test 행을 삭제했습니다. GA4는 Preview 전용 속성·스트림·Vercel 브랜치 환경에서만 활성화해 DebugView와 동의 철회를 검증했고, 광고 개인 최적화는 307개 전 지역에서 차단했습니다. Production GA4는 비활성 상태이며 인증 브라우저 진단 전송 종단간 검증, 보관 스케줄러와 개인정보처리방침 게시가 남아 있습니다.
- 결과: 제품 사용성은 선택 동의의 GA4로, 최소 장애 원인은 별도 Supabase 저장소로 관찰할 수 있습니다. 실제 GA4 속성·환경변수·개인정보처리방침과 Preview 검증을 마치기 전에는 제품 분석을 운영에서 활성화하지 않습니다.

## DEC-061 — 완료 라운드는 요약 우선, 상세 원본은 필요할 때 로드

- 상태: 확정
- 맥락: 로그인 직후 모든 라운드 원본과 모든 비거리 이력을 내려받고, 한 라운드 변경에도 전체 라운드를 다시 저장하면 개인 기록이 늘수록 읽기·쓰기 비용이 함께 증가합니다. 다만 오프라인 복원과 과거 기록 보존 때문에 원본을 성급히 제거할 수는 없습니다.
- 결정: 작성 중 라운드는 즉시 이어쓰기를 위해 원본을 우선 로드하고, 완료 라운드는 홈에 날짜·코스·스코어·통계 요약만 먼저 로드합니다. 완료 상세는 사용자가 열 때 해당 라운드 `payload`를 한 건 조회해 기기에 캐시합니다. 비거리는 최신 세트를 초기 로드하고 전체 이력은 DB에 보존합니다. 저장은 변경된 라운드만 전송합니다.
- 결정: `rounds.payload`를 원본으로 유지하고 요약 컬럼은 재생성 가능한 파생 캐시로 둡니다. migration은 컬럼·인덱스 추가와 기존 원본 기반 backfill만 수행하며 원본을 삭제하지 않습니다. 프로필·라운드·클럽 조회는 독립 처리하고 자동 재시도는 최대 1회로 제한합니다.
- 결과: 홈과 일반 저장은 기록 총량에 비례해 계속 무거워지지 않으며, 이전 기록·오프라인 초안·전체 비거리 이력을 잃지 않습니다. 과거 목록 페이지네이션과 비거리 이력 열람 UI는 실제 데이터 규모를 확인한 뒤 추가할 수 있습니다.

## DEC-062 — UI worktree는 최신 검증 기준에서 짧게 운영

- 상태: 확정
- 맥락: 기능 브랜치와 분리된 UI worktree가 오래되면서 Google 로그인과 Beta 승인 요청처럼 이미 구현된 기능이 없는 화면을 기준으로 디자인 작업을 시작하는 문제가 발생했습니다.
- 결정: UI·문구·폰트·레이아웃 worktree는 작업 시작 직전에 최신 검증 통합 기준으로 새로 만들거나 갱신합니다. 수정 전 로그인 화면, Google 로그인 호출, Beta 승인 요청, 홈 진입의 최소 스모크 테스트를 통과해야 합니다. UI 작업자는 기능을 재구현하거나 오래된 화면을 임의로 복구하지 않습니다.
- 결과: 기능과 디자인을 병렬로 진행하되 오래된 분기에서 누락 기능을 다시 만드는 중복 작업을 피합니다. 장기 기능 작업이 끝나기 전에는 기존 UI worktree에 추가 수정을 쌓지 않습니다.

## DEC-063 — 골프장 기준 거리와 실제 플레이 거리를 분리 보존

- 상태: 확정
- 맥락: 연결된 골프장의 홀·티 거리 정보를 불러와도 당일 티 위치, 임시 티잉 구역과 현장 표기 때문에 실제 플레이 거리가 달라질 수 있습니다. 사용자가 홀 거리값을 수정했다고 해서 검증된 공용 골프장 원본을 함께 바꾸면 다른 라운드와 사용자의 기준 데이터가 오염됩니다.
- 결정: 골프장 DB의 `sourceDistanceMeters`·`sourceDistanceYards`는 기준 원본으로 보존하고, 라운드의 `distance`는 해당 라운드에서 사용자가 확인·수정한 실제 플레이 거리로 저장합니다. 직접 수정한 값은 `distanceSource: user`, 원본 그대로면 `distanceSource: course_database`로 구분하며 사용자 수정값을 공용 골프장 마스터에 자동 반영하지 않습니다.
- 결정: 향후 거리 통계는 골프장 기준 거리와 실제 플레이 거리를 섞지 않습니다. 라운드 분석에는 실제 플레이 거리를 우선 사용하고, 출처별 비교가 필요하면 기준 거리·플레이 거리·차이·골프장 데이터 버전을 별도 필드 또는 파생값으로 다룹니다. 공용 원본 수정은 별도의 골프장 정보 정정 요청과 검증 절차를 거칩니다.
- 결과: 현장 입력은 실제 플레이를 정확히 반영하면서도 골프장 기준 원본과 과거 라운드의 재현성을 함께 지킵니다.
