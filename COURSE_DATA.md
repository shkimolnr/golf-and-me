# 골프장 코스 데이터

## 운영 원본

- 사람이 계속 보강하는 원본: `outputs/golf-yardage-db/골프와나_골프장마스터_DB_v3.xlsx`
- 앱에서 읽는 정규화 데이터: `src/data/golfCourseDatabase.json`
- 앱 연결 로직: `src/data/courseData.js`
- 현재 범위: 6개 골프장, 14개 코스 구간, 144개 홀, 19개 별칭

## 변환 규칙

- 엑셀의 `없음`은 앱 DB에서 `null`로 변환하며 추정값을 넣지 않습니다.
- `홀`은 원본 골프장의 공식 홀번호를 그대로 저장하며 임의로 재번호화하지 않습니다.
- 티 색상은 블랙·블루·화이트·골드·레드 순서로 고정하고 Orange는 골드로 정규화합니다.
- 원본 단위를 보존하고 `1m = 1.0936133yd`로 환산한 반대 단위를 함께 둡니다.
- 골프장별 대표 출처, 최초 수집일, 마지막 확인일을 관리합니다.
- HCP는 선택 데이터이며 제공되지 않은 경우 `null`입니다.

## 앱 연동 원칙

- 이름 일부가 별칭과 일치하면 `[홀 정보]` 자동완성 후보를 표시합니다.
- 사용자가 후보를 직접 선택해야만 골프장 ID가 연결됩니다. 자유 입력만 한 골프장은 자동 연결하지 않습니다.
- 연결된 골프장은 전·후반 코스를 드롭다운으로 고르고 티와 M/YD 단위에 맞는 PAR·홀 거리를 채웁니다.
- 자동 입력된 PAR와 거리는 홀 기록 화면에서 수정할 수 있습니다.
- 앱 내부의 플레이 순서 1–18과 공식 홀번호를 분리합니다. 예를 들어 IN→OUT 라운드는 플레이 순서와 무관하게 화면에 공식 `10–18 → 1–9`를 표시합니다.
- TIGER·LION처럼 한 코스가 공식 1–18번 전체를 가진 경우 원본 코스명·홀번호는 변경하지 않습니다. 앱에서만 `TIGER OUT`(1–9번), `TIGER IN`(10–18번) 같은 구간을 파생하고, 선택한 두 구간을 플레이 순서에 연결합니다.
