# 📄 골프와 나 PRD v2.1
> 최초 작성: 2025.06 | 업데이트: 2026.08.30

---

## 1. 서비스 개요

| 항목 | 내용 |
|---|---|
| **서비스명** | 골프와 나 |
| **슬로건** | 당신의 골프 성장 파트너 |
| **형태** | 모바일 웹 (PWA) |
| **목표 사용자** | 실력 향상에 관심 있는 아마추어 골퍼 |
| **핵심 가치** | 매홀 쉽고 편한 기록 → 라운드 후 깊은 분석 → 맞춤 연습 방향 제시 |

### 서비스 철학
- 단순 스코어카드 앱이 아닌 **개인 맞춤형 골프 성장 플랫폼**
- 기록은 간단하게, 분석은 깊게
- 모바일 편의성 + 아날로그 노트 감성 결합
- 고가 하드웨어 없이 스마트폰만으로 AI 캐디 수준의 가치 제공

---

## 2. 기술 스택

| 영역 | 도구 | 결정 이유 |
|---|---|---|
| **인증** | 구글 로그인 (OAuth 2.0) | 개발 속도 우선, 카카오 추가는 Phase 2 검토 |
| **프론트엔드** | React + Vite + CSS | 모바일 우선 화면을 빠르게 반복하고 정적 앱 셸로 배포 |
| **백엔드/DB** | Supabase (PostgreSQL) | 복잡한 통계 쿼리, Edge Functions으로 외부 연동 |
| **푸시 알림** | Phase 2 검토 | Alpha에서는 기록·복구 흐름에 집중 |
| **외부 알림 (Phase 2)** | 이메일 제공자 추후 선정 | 정기 리포트와 수신 동의 정책 확정 후 도입 |
| **호스팅** | Vercel | GitHub `main` 자동 배포, Preview/Production 분리와 고정 HTTPS 운영 |

### 기술 스택 주요 결정 배경
```
Supabase 유지 근거:
- 구글 로그인 서비스명 표시 → OAuth 동의 화면 설정으로 해결
- 외부 알림 연동 → Supabase Edge Functions으로 해결
- MAU 100만명 이하 → Supabase Pro/Team 플랜으로 충분
- GCP 전환 시 개발 복잡도 2~3배 증가 → 불필요

단계별 Supabase 요금:
MVP → Free 플랜 (MAU 5만명 이하)
성장기 → Pro ($25/월)
안정기 → Team ($599/월)
```

---

## 3. 색상 가이드

| 용도 | 색상 | 코드 |
|---|---|---|
| 기본 배경 | 흰색 | #FFFFFF |
| 기본 텍스트 | 진한 회색 | #2D3748 |
| 강조 (메인) | 골프 그린 | #059669 |
| 서브 텍스트 | 회색 | #6B7280 |
| 베스트/특별 하이라이트 | 골드 | #B7791F |
| 보조 액션 | 네이비 | #2B6CB0 |
| 카드 배경 | 라이트 그레이 | #F7FAFC |

**디자인 톤**: 차분하고 고급스러운 스타일, 여백의 미 강조

**타이포그래피**: 직접 제작한 `Griun Cherrie`를 브랜드와 앱 전반의 기본 서체로 적극 사용하며, 로딩 실패 시 시스템 한글 폰트로 대체합니다.

---

## 4. MVP 화면 목록

| 순서 | 화면명 | 비고 |
|---|---|---|
| 1 | 온보딩 | 최초 로그인 시 기본 티·거리 단위·클럽 구성 확인 |
| 2 | 로그인 | 구글 로그인 |
| 3 | 홈 화면 | 나의 라운드 리포트 + 메인 CTA |
| 4 | 새 라운드 생성 | 골프장/코스/티 입력 |
| 5 | 스코어카드 메인 | 전반/후반 탭 + 3×3 홀 그리드 |
| 6 | 홀별 상세 기록 | 스코어/OB/페널티/FIR/GIR/퍼팅 |
| 7 | 라운드 요약/리포트 | 분석 + AI 코치 총평 |
| 8 | 클럽별 거리 관리 | 기본 티그라운드 설정 포함 |
| 9 | 계정 관리 | 로그아웃, 계정과 전체 기록 영구 삭제 |

---

## 5. 화면별 상세 설계

### 5-1. 온보딩 (최초 로그인 시)

```
구글 로그인 완료 후 최초 1회만 진행:

Step 1: 환영 메시지
"골프와 나에 오신 것을 환영합니다"
"당신의 골프 성장 여정을 시작해볼게요"

Step 2: 기본 티그라운드·거리 단위 설정
"주로 어떤 티에서 플레이하시나요?"
⚫ 블랙티 / 🔵 블루티 / ⚪ 화이트티 / 🟡 골드티 / 🔴 레드티

Step 3: 현재 골프백의 클럽 구성 확인
- 실제 사용하는 클럽을 선택하고 목록에 없으면 직접 추가
- 클럽 구성을 완료해야 첫 라운드를 기록할 수 있음

클럽 구성을 확인하면 별도의 비거리 화면을 거치지 않고 홈으로 이동
```

---

### 5-2. 홈 화면

```
[헤더]
🏌️ 골프와 나 (좌측, 24px Bold)     [김] [⚙️] (우측)
당신의 골프 성장 파트너

[메인 CTA]
⛳ 새 라운드 기록하기 (그린 배경, 가장 큰 버튼)

[나의 라운드 리포트]               전체보기
┌──────┬──────┬──────┐
│ 87.3  │  12  │  42% │
│ HC 15 │      │      │
│ 평균  │  총  │  GIR │
│스코어 │라운드│  평균 │
├──────┼──────┼──────┤
│  3.2  │  67% │🏆82타│
│ 평균  │  FIR │ 베스트│
│  퍼팅 │  평균 │스코어│
└──────┴──────┴──────┘

[최근 라운드]
└─ 용인컨트리클럽 동코스  9.01(일)
   85타 (+13)            ⛅ 흐림 22°C

[클럽별 거리 관리] (중요 기능 버튼)

[풋터]
© 2025 골프와 나. All rights reserved.
더 나은 골프를 위한 여정이 시작됩니다
```

**핵심 설계 원칙:**
- 메뉴명: "나의 라운드 리포트" (감성 + 분석 기대감)
- 통계 3×2 그리드 구조
- 최근 라운드 1개 + "전체보기" 링크
- 클럽별 거리 관리는 설정이 아닌 핵심 기능으로 분류
- 헤더의 사용자 이니셜 또는 프로필 이미지를 누르면 계정 메뉴를 바텀시트로 표시
- 계정 메뉴에는 사용자 이름, Google 이메일, 명시적인 로그아웃 버튼을 제공하며 프로필 아이콘을 눌러 즉시 로그아웃하지 않음
- 설정 아이콘은 기본 티그라운드와 클럽별 거리 관리로 연결하되 해당 화면 구현 시 활성화

---

### 5-3. 새 라운드 생성

**입력 항목:**
```
필수:
- 골프장명 (자유 입력 또는 `[홀 정보]` 자동완성 후보 선택)
- 전반 코스명과 후반 코스명 (연결 골프장은 드롭다운, 미연결 골프장은 자유 입력)
- 티그라운드: ⚫블랙 / 🔵블루 / ⚪화이트 / 🟡골드 / 🔴레드
  → 기본 티그라운드 설정값이 자동 선택된 상태로 화면 오픈
  → 필요 시 변경 가능

자동:
- 날짜/시간 (현재 시간 기본값)
- 날씨 (API 연동)

선택:
- 동반자 메모
```

**라운드 구조 정보 수정 정책:**
- 골프장명과 전·후반 코스는 세 번째 플레이 홀까지 변경 가능
- 네 번째 플레이 홀에 저장 기록이나 의미 있는 임시 입력이 생기면 구조 정보 잠금
- 초기 홀 기록이 있는 상태에서 구조 정보를 변경하면 `기록 유지하고 코스만 변경`과 `홀 기록 지우고 다시 시작` 중 선택
- 기록 유지 시 샷·클럽·트러블·퍼팅은 플레이 순서에 유지하고 새 홀번호·PAR·거리를 연결해 통계 재계산
- 다시 시작 시 홀 저장 기록과 임시 초안만 삭제하고 골프장·코스·날짜·티 정보는 유지
- 완료 라운드는 골프장·코스를 잠그고 날짜·시간·티·단위·동반자 메모만 수정

**골프장 DB 운영 방침:**
```
Phase 1 (MVP): 단일 시트 누적형 엑셀을 운영 원본으로 사용하고 검증된 골프장을 앱용 JSON으로 변환
                 `[홀 정보]` 후보를 명시적으로 선택한 경우에만 PAR·티별 거리를 연결
                 연결된 새 라운드는 시작 전에 골프장·전후반 코스·티·단위를 한 번 확인
                 미연결 골프장은 전반 1~9·후반 10~18로 생성하고 PAR 우선 수동 입력과 개인 입력 이력 자동완성을 유지
Phase 2: 검증 골프장 확대, 사용자 입력 후보의 중복·별칭·검증 상태 관리
         전국 단위 데이터 소싱과 위치 좌표 운영 방식 검토
Phase 3: GPS 자동 매칭 연동
         (새 라운드 시작 시 위치 1회 요청
          → 반경 500m~1km 내 골프장 자동 매칭
          → 좌표는 저장 안 함 / 법적 이슈 없음)
```

**야디지 DB 운영 방침:**
```
DB 있을 때:
→ 골프장 선택 시 홀별 야디지 자동 입력
→ 티그라운드별 거리 자동 반영

DB 없을 때:
→ 야디지 입력 생략 가능 (선택사항)
→ 스코어 기록은 정상 작동

설계 원칙:
"야디지는 선택사항, 있으면 더 좋은 경험"
→ holes 테이블 distance 컬럼 nullable 처리
```

---

### 5-4. 스코어카드 메인

**구조:**
```
[상단 고정] 라운드 기본 정보
골프장명 / 코스명
📅 날짜   🏁 티그라운드   ☀️ 날씨

[탭] [전반 · 입력한 전반 코스명] [후반 · 입력한 후반 코스명]

[3×3 홀 그리드]
┌──────┬──────┬──────┐
│  1   │  2   │  3   │
│ PAR4 │ PAR5 │ PAR3 │
│  [+] │  5   │  [+] │
├──────┼──────┼──────┤
│  4   │  5   │  6   │
│ PAR4 │ PAR4 │ PAR3 │
│  [+] │  [+] │  [+] │
├──────┼──────┼──────┤
│  7   │  8   │  9   │
│ PAR5 │ PAR4 │ PAR4 │
│  [+] │  [+] │  [+] │
└──────┴──────┴──────┘

[하단 요약]
전반 합계: 0  /  전체 합계: 0  /  파대비: E
```

**홀 카드 상태:**
```
미입력: 파 정보는 `PAR -`, 스코어는 [+] 기호 → 터치 시 홀별 상세 입력 화면
입력완료: 스코어 숫자 + 상태 뱃지
          (이글🦅 / 버디🐦 / 파 / 보기 / 더블보기 등)
```

**코스 정보가 없는 Alpha의 파 입력:**
```
- 새 라운드 생성 시 홀별 파는 추정하지 않고 null로 생성
- 홀 상세 기록에서 PAR 3 / 4 / 5 / 6 중 직접 선택
- 파 정보가 입력된 홀만 파 대비 계산에 포함
- 연결된 골프장은 선택한 코스·티·단위 기준의 PAR와 홀 거리를 자동 입력하고 사용자 수정값은 보존
- 공식 18홀 코스는 원본 코스명·홀번호를 보존하고 앱 선택지에 `코스명 OUT`(1–9번)과 `코스명 IN`(10–18번)을 파생
```

---

### 5-5. 홀별 상세 기록

**수집 데이터 (MVP 필수):**

```
기본 정보:
- 홀 번호, 파
- 야디지 (DB 있으면 자동, 없으면 선택 입력)
- 티샷 행의 홀 거리 (현재 직접 입력, 야디지 DB 도입 시 선택한 티 기준 자동 입력)

티샷 기록:
✅ FIR - 페어웨이 적중 여부 (파4/파5만 해당)
✅ 미스 방향 (왼쪽/오른쪽/짧음/넘어감)
✅ OB 처리 → 재샷은 실제 재샷 +1벌타, 전진 구제는 +2벌타
✅ 페널티 구역 여부 → 화면에는 `페널티`, 계산은 +1벌타
✅ 사용한 샷의 클럽 선택 (홀 완료 조건, 임시 저장에는 불필요)

세컨샷 이후:
✅ GIR - 그린 적중 여부
✅ 퍼팅 수
✅ OB/페널티 여부 (세컨샷 이후에도 발생 가능)
□ 남은 거리 (선택사항)
✅ 사용한 샷의 클럽 선택 (홀 완료 조건, 임시 저장에는 불필요)
□ 미스 방향 (선택사항)

보조 기록 (선택사항):
□ 텍스트 메모
□ 감정 체크
```

**자동 계산:**
```
스코어 = 스코어에 포함되는 실제 샷 수 + 퍼팅 수 + 벌타 합계
FIR% = 페어웨이 적중 홀 / FIR 값이 기록된 파4+파5 홀
GIR% = 그린 적중 홀 / GIR 값이 기록된 홀
평균 퍼팅 = 총 공식 퍼팅 수 / 퍼팅 값이 기록된 홀
```

**결측값과 분모 정책:**

- FIR은 파3를 제외하고 FIR 값이 확정된 파4·파5만 분모에 포함
- GIR과 퍼팅은 실제 값이 있는 홀만 분모에 포함
- 점수와 PAR가 모두 있는 홀만 파 대비와 스코어 결과 분포에 포함
- 표본이 없으면 `0%`나 `0회` 대신 `—` 표시
- 미입력 홀·샷 거리는 0m로 간주하지 않으며 거리 기반 통계에는 실제 표본 수를 함께 표시

**OB/페널티 UX:**
```
인라인 선택 방식:
[OB] 버튼 탭 → [재샷 +1] 또는 [전진 구제 +2] 선택
[페널티] 버튼 탭 → +1벌타 즉각 반영
복수 발생 시 횟수 추가 가능
```

**저장과 완료:**

- 퍼팅 선택은 계산값만 갱신하며 홀 완료를 자동 확정하지 않음
- 신규 입력 화면에는 `임시 저장`과 `홀 기록 완료`를 함께 제공
- `홀 기록 완료`는 PAR, 실제 샷, 퍼팅과 OB 처리의 계산 조건이 충족된 경우에만 활성화
- 티샷부터 마지막 사용 샷까지 중간 공백이 없고 사용한 모든 샷에 클럽이 선택되어야 완료 가능
- 버튼 위에 `샷 n · 퍼팅 n · 벌타 n = 총 n타` 계산 근거를 항상 표시하며 희귀하거나 좋은 스코어 자체는 경고하지 않음
- 홀거리·남은 거리 누락은 거리 분석 제외를 부드럽게 안내하되 완료를 막지 않음
- 사용하지 않은 후속 샷 행은 누락 경고 대상이 아님

---

### 5-6. 라운드 요약/리포트

```
완료 전환:
- 홀 저장과 라운드 완료를 분리
- 18홀을 모두 저장하면 스코어카드의 `라운드 완료` 활성화
- 미입력 홀이 있으면 해당 홀 번호를 안내하고 완료 불가
- 최종 확인에서 18/18홀, 파 대비, 총 타수를 확인한 뒤 결과 화면으로 이동
- 완료 후 홀 기록 수정 시 결과와 통계를 즉시 재계산

완료 직후 결과 화면:
- 최종 스코어는 `파 대비 / 총 타수`를 한 셀의 최상단 핵심 정보로 표시
- 같은 줄 오른쪽에 전반·후반의 `파 대비 / 타수`를 더 작은 크기로 표시
- 다음 줄에는 실제 발생한 파 대비 결과만 좋은 순서로 나열하고 0개 항목은 숨김
- 홀인원은 파 대비 결과(예: 파3 이글)와 중복 계산하지 않고 별도 특별 기록으로 강조
- FIR·GIR은 성공 수/적용 홀 수와 비율을 함께 표시
- 총 벌타와 함께 OB·패널티 발생 횟수를 분리해 표시
- 총 퍼팅·홀당 평균과 1퍼트·2퍼트·3퍼트 이상 홀 분포 표시
- 값이 없는 홀은 `TASK-002`의 확정 정책에 따라 해당 통계 분모에서 제외
- 저장·이관된 과거 기록에 PAR 누락이 있으면 플레이 요약 제목 옆에 누락 홀 수와 파 대비·스코어 분포 제외 사실을 경고
- 스코어카드 보기, 라운드 정보 수정, 완료 기록 삭제 제공

단일 라운드 분석:
- 총 스코어, GIR%, FIR%, 평균 퍼팅
- OB/페널티 발생 홀 및 횟수
- 미스 방향 패턴 (왼쪽/오른쪽 비율)
- 클럽별 사용 현황

AI 코치 총평 (Phase 2):
- 넬리 (긍정/격려형)
- 로리 (엄격/분석형)
```

---

### 5-7. 클럽별 거리 관리

```
기능:
- 1단계에서 현재 골프백의 클럽 구성을 선택하고, 목록에 없는 클럽은 짧은 표시명을 직접 추가
- `내 골프백`에서 최초 클럽 구성을 완료하면 비거리 화면으로 이동하며, 완료 후 재진입은 사용 빈도가 높은 비거리 화면을 기본으로 표시
- 신규 사용자는 드라이버 D와 퍼터 PT만 기본 선택하고 나머지는 직접 고르며, 추천 구성을 임의로 채우지 않는다
- 클럽 개수·종류는 제한하지 않고 사용자가 현재 구성을 직접 확인해 저장했는지를 구성 완료 기준으로 사용한다
- 온보딩은 클럽 구성 확인으로 끝내고 비거리 입력 화면을 포함하지 않는다. 비거리 기록이 없는 사용자가 새 라운드를 시작할 때 선택 기능이 있음을 짧게 안내하고 원하면 `내 골프백` 비거리 화면으로 이동한다
- 클럽 구성을 아직 확인하지 않은 계정은 새 라운드 생성 전에 클럽 등록 안내를 표시한다
- 구성 완료 후 `클럽 구성` 탭은 선택된 클럽만 읽기 전용으로 표시하고, 사용자가 `클럽 수정하기`를 선택한 경우에만 전체 선택 목록과 직접 추가 버튼을 다시 표시
- 기존 클럽 구성을 수정·저장하면 `클럽 구성` 완료 화면으로 돌아가 변경 결과를 먼저 확인하며, 최초 구성 완료 때만 `클럽별 비거리`로 자동 이동
- 표기: 드라이버 D, 우드 3W, 유틸리티 3UT, 아이언 7I, 웨지는 P/A/S 또는 로프트 숫자
- 표 순서: 드라이버 → 우드 → 유틸리티 → 아이언 → 웨지로 고정하고 왼쪽 열을 위에서 아래로 채운 뒤 오른쪽 열로 이어서 표시
- 2단계에서 한 시점에 사용하는 전체 클럽 비거리 세트를 등록
- 특정 클럽만 새로 입력해도 같은 단위·거리 기준의 나머지 클럽은 직전 세트 값을 이어받아 완전한 세트를 생성
- 새로 입력한 클럽만 강조하고 이전 전체 세트 데이터는 보관하되, MVP 화면에서는 최신 세트만 조회
- 과거 이력 조회는 달력에서 최근 약 10개 세트를 선택하는 안을 후속 검토하며 MVP에서는 생략
- 사용자가 저장한 이전 클럽 비거리 세트는 향후 추세 분석과 이력 조회 가능성을 위해 작성 시점별로 삭제·덮어쓰기 없이 보존한다.
- 단위 변경 시 기존 비거리는 M↔YD로 환산하고, 거리 기준은 선택 사항인 `미지정 / 캐리 / 총거리`로 구분한다. 단위는 환산 가능하지만 서로 다른 거리 기준의 값은 자동으로 섞지 않는다.
- 로그인 사용자의 클럽 구성과 모든 비거리 세트는 계정 데이터로 동기화한다. 구성에서 제거된 클럽도 과거 세트 재현을 위해 비활성 상태로 보존하고, 동일 세트는 최신본을 사용하며 서로 다른 세트는 모두 유지한다.
- 저장 이력이 없어도 선택한 클럽 전체를 `클럽 | 비거리 | 클럽 | 비거리` 표에 표시하고 값은 `—`로 안내
- 거리 기준은 캐리 거리/런 포함 중 선택할 수 있으며 기본은 미선택
- 비거리 값은 선택사항이며 빈 값은 0으로 바꾸지 않음

단위:
- M/YD 사용자 기준을 표시하고 추후 온보딩·계정 기본 단위와 연결
- 단위 또는 거리 기준이 달라지면 이전 숫자를 자동 승계하지 않음

데이터 구조:
거리세트 | 입력일시 | 단위 | 거리기준 | 클럽구성[] | 클럽별거리{} | 이번 변경 클럽[]
```

---

## 6. 데이터 모델

```sql
users
- id
- email
- name
- default_tee (enum: black/blue/white/gold/red)
- default_tee_updated_at
- created_at

rounds
- id
- user_id
- course_name
- course_type
- tee_type (enum: black/blue/white/gold/red)
- date
- weather
- total_score
- created_at

holes
- id
- round_id
- hole_number
- par
- score (자동 계산)
- putts
- fir (boolean, 파3 제외)
- gir (boolean)
- ob_count (OB 횟수)
- penalty_count (페널티 횟수)
- miss_direction (enum: left/right/short/over)
- club_used (nullable)
- distance (nullable - 야디지 DB 없으면 null)
- memo (nullable)

clubs
- id
- user_id
- club_name
- current_distance
- measured_at
- history (이전 거리 배열)

-- Phase 2 추가 예정
golf_courses
- id
- name
- location
- latitude (GPS 매칭용)
- longitude (GPS 매칭용)

course_holes
- id
- course_id
- hole_number
- par
- distance_black
- distance_blue
- distance_white
- distance_gold
- distance_red
- handicap_index
```

**Alpha 서버 저장 구현:**
- `profiles`: 기본 티·거리 단위와 온보딩 완료 여부 저장
- `rounds`: 검색용 핵심 필드와 현재 라운드 전체 원본을 `payload JSONB`로 함께 저장
- `round_holes`, `round_shots`: `rounds.payload` 저장 시 DB 트리거로 자동 정규화
- `user_clubs`, `club_distance_history`: 클럽별 현재 구성과 거리 이력을 분리
- 라운드 샷에는 선택 당시 클럽 고유 ID·표시명 스냅샷을 함께 저장해 골프백 변경 후에도 과거 기록을 재현
- 입력은 로컬에 먼저 저장하고 로그인된 사용자의 서버 데이터와 `updatedAt` 기준으로 병합
- 모든 사용자 소유 테이블에 `auth.uid()` 기반 RLS 적용

**Beta 로그인 성능 검증:**
- 로그인 시작부터 `session_restored`, `records_ready`까지의 단계별 시간을 측정하되 최근 20건은 기기에만 보관한다.
- 2026-08-31 실제 모바일 영상 점검 결과 앱 복귀 후 사용 가능한 화면까지 약 0.1~0.5초로 확인해 현재 MVP 성능 점검은 통과 처리한다.
- GTM 없이 GA4 웹 데이터 스트림과 사용자 동의가 활성화된 뒤에만 개인정보 없이 `duration_ms`의 실제 추세를 확인한다. Development·Preview·Production은 측정 ID와 활성화 여부를 분리하고, Preview가 Production 속성으로 초기화되지 않게 한다.
- Google 계정 선택창 표시와 사용자의 계정 선택 대기시간은 외부 인증·사용자 조작 구간으로 분리해 앱 준비 시간과 혼합하지 않는다.
- 재로그인 뒤 원격 기록 조회가 실패한 관찰 사례만으로 데이터 양을 원인으로 단정하지 않는다. 프로필·라운드·클럽/비거리 조회를 분리해 네트워크·인증 세션·Supabase 응답·전송량과 소요 시간을 확인한 뒤 대응한다.
- 라운드와 비거리 이력은 계속 증가하므로, 초기 홈은 필요한 요약·최근 기록을 우선 준비하고 과거 완료 라운드와 오래된 거리 이력은 페이지·시점 단위로 나누어 불러오는 구조를 검토한다. 구체적인 전환 기준과 데이터 정합성 검증은 `TASK-046`에서 확정한다.

**Beta 제품 분석:**
- 제품 이용 흐름은 GA4 직접 연동으로 분석한다. 사용자가 온보딩 전 선택 화면 또는 계정 메뉴에서 허용한 뒤에만 `gtag.js`를 로드하며, 거부·철회해도 모든 핵심 기능은 정상 동작한다. 자동 page view 대신 SPA 화면 전환과 허용된 제품 이벤트를 명시적으로 보낸다.
- GA4에는 익명 화면명, 기능 이벤트, 단계·성공 여부, 완료 홀 수, 온라인 여부와 제한된 `duration_ms`만 보낸다. 이메일·사용자 UUID·Google 정보·토큰·전체 URL·골프장/코스·점수·홀/샷/클럽 원본·메모·자유 입력·원본 오류/stack은 포함하지 않는다.

**MVP 운영 오류 진단:**
- 운영 오류는 GA4와 분리한 Supabase `app_diagnostics`로 처리한다. 인증된 Vercel API가 난수 incident ID, 실패 단계·정규화된 오류 분류·안전한 HTTP 상태, 앱 버전·일반화된 플랫폼·온라인 여부, 발생·복구 시각·횟수만 검증해 저장한다. 브라우저가 테이블에 직접 쓰지 않으며, 인증에 사용한 사용자 UUID는 DB·로그에 남기지 않는다.
- 현재는 OAuth, 프로필·라운드·클럽 조회/저장, 라운드 삭제와 계정 삭제를 연결한다. 같은 incident는 최신 시각과 횟수만 갱신하고, 전송 실패는 최대 20건의 안전한 기기 큐로 제한해 기록 기능을 막지 않는다. 로그인 전 OAuth 실패는 인증된 요청만 허용하는 정책상 중앙 진단에 전송하지 않는다.
- 원시 진단은 30일, 복구된 incident는 7일 뒤 삭제하는 기본값을 둔다. 실제 마이그레이션·Vercel Secret·보관 스케줄러·개인정보처리방침·Preview 검증 전에는 운영 수집을 활성화하지 않는다. 상세 추적·분산 추적·알림·대시보드는 실제 장애 패턴을 확인한 뒤 후속 단계로 확장한다.

---

## 7. 핵심 기능 로드맵

### Phase 1 (MVP)
```
✅ 구글 로그인
✅ 온보딩 (기본 티·거리 단위·클럽 구성 구현 및 실제 Supabase 마이그레이션 완료)
✅ 새 라운드 생성 (수동 입력)
✅ 스코어카드 (전반/후반 탭 + 3×3 그리드)
✅ 홀별 기록 (스코어/OB/페널티/FIR/GIR/퍼팅/미스방향)
✅ 자동 스코어 계산 (벌타 반영)
✅ 누적 통계 (홈 화면 대시보드)
✅ 클럽별 거리 관리
✅ 로컬 우선 저장·계정 동기화·입력 복구
```

### Phase 2
```
⬜ 검증 골프장 확대와 전국 단위 데이터 소싱·검증 체계 구축
⬜ 규칙 기반 클럽 추천 (거리 기반 패턴 분석)
⬜ AI 코치 (넬리/로리) - LLM API 연동
⬜ 스코어 트렌드 그래프
⬜ 티별 통계 필터
⬜ 날씨·감정 맥락 분석
⬜ 이메일 정기 리포트와 수신 설정
⬜ 카카오 알림톡 (사업자 등록 후)
⬜ FCM 푸시 알림 활성화
```

### Phase 3
```
⬜ GPS 골프장 자동 매칭
⬜ [실전팁] 기능 - 홀 진입 시 AI 조언
   (티샷 방향 패턴, 클럽 추천 등)
⬜ 야디지 DB 확장 (파트너십 또는 유료 데이터 포함)
⬜ 날씨/코스 복합 분석
⬜ 카카오 로그인 추가
```

### Phase 4
```
⬜ 블루투스 거리측정기 연동
   (부쉬넬 Pro X3+ LINK / 가민 어프로치 등)
⬜ 실시간 AI 캐디 (거리 자동 수신 + 즉각 조언)
⬜ 머신러닝 기반 패턴 분석
⬜ 사용자 간 공유 / 소셜 기능
⬜ Native App 전환 검토
```

---

## 8. UX 원칙

```
1. 최소 입력 우선
   → 필수 항목만 입력해도 기록 완료
   → 선택 항목은 언제든 추가 가능

2. 비순차 입력 허용
   → 홀 순서 강제 없이 원하는 홀로 이동
   → 깜빡한 홀 나중에 입력 가능
   → 잘못 입력한 홀 언제든 수정 가능

3. 실물 감성
   → 종이 스코어카드가 펼쳐지는 느낌
   → 전반/후반 탭으로 자연스러운 구분

4. 즉각 피드백
   → OB/페널티 입력 시 벌타 즉각 반영
   → 스코어 자동 계산 실시간 표시
   → 퍼팅 입력 전에는 마지막 샷 결과를 추정하지 않고, 시작 위치 선택 후 온 그린/엣지/홀인을 판정

5. 사후 분석 중심
   → 라운드 중: 스코어 + 핵심 데이터만
   → 라운드 후: 충분한 분석 및 연습 방향 제시
```

---

## 9. 경쟁 서비스 현황

```
부쉬넬 Pro X3+ LINK:
→ 블루투스 거리측정기 + 앱 연동
→ 클럽 추천, 바람/기온 통합 분석
→ 국내 가격 약 70~90만원대 (하드웨어 필요)

가민 어프로치 Z82:
→ 레이저 + GPS 결합 + 가민 골프 앱 연동
→ 스마트워치 동기화
→ 국내 가격 약 60~80만원대

차별화 전략:
→ 고가 하드웨어 없이 스마트폰만으로 동급 가치 제공
→ 장기적으로 연동 파트너십 가능성 검토 (Phase 4)
```

---

*작성: 2025.09 | 골프와 나 개발 프로젝트 | v2.1*
