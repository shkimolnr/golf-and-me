# Golf & Me 작업 장부 운영 원칙

기준일: 2026-09-03
기술 기준선: GitHub·앱 Production `26896d5` / DB Production `TASK-051` migration 002·backfill 적용 완료

이 문서는 업무 내용을 다시 적는 목록이 아니라, 새 요청과 발견 사항이 누락·왜곡되지 않도록 관리하는 규칙의 원본입니다.

## 1. 문서별 단일 역할

| 문서 | 단일 역할 | 하지 않는 일 |
|---|---|---|
| `ISSUE_INBOX.md` | 들어온 이슈의 원문 요약과 이후 분류 이력을 보존 | 실행 우선순위나 완료를 단독 확정하지 않음 |
| `BACKLOG.md` | 실행할 태스크, 우선순위, 상태, 완료 조건의 유일한 원본 | 접수 원문을 대신하지 않음 |
| `DECISIONS.md` | 사용자가 확정한 제품·기술·운영 정책을 시간순으로 보존 | 미확정 아이디어를 확정처럼 기록하지 않음 |
| `OPERATIONS.md` | 현재 실제 환경과 운영 절차를 기록 | 미래 계획을 현재 상태처럼 기록하지 않음 |
| `PRD.md` | 현재 제품 요구사항의 원본 | 개별 작업 진행 상황을 관리하지 않음 |
| `PROJECT_RULES.md` | 계속 지켜야 할 개발·제품 원칙 | 일회성 작업 내역을 쌓지 않음 |

## 2. 절대 규칙

1. 새 이슈에는 순번이 고정된 `ISSUE-NNN`을 즉시 부여합니다.
2. 접수 행은 삭제하거나 번호를 재사용하지 않습니다. 사실 오류는 원문을 덮어쓰지 않고 새 라우팅 이벤트로 정정합니다.
3. 모든 이슈에는 최소 한 개의 `ROUTE-NNN`이 있어야 합니다.
4. 우선순위·상태·완료 조건은 `BACKLOG.md`에서만 바꿉니다.
5. 재분류는 이전 기록을 고치지 않고 새 라우팅 이벤트를 추가합니다.
6. 태스크 완료에는 코드뿐 아니라 검증 증거와 관련 문서 현행화가 필요합니다.
7. Production, DB, 인증, 외부 서비스, 개인정보 정책 변경은 사용자의 명시적 승인 없이 실행하지 않습니다.
8. 작업자가 범위 밖 문제를 발견하면 임의 구현하지 않고 이슈로 돌려보냅니다.

## 3. 처리 흐름

```text
접수 → 영향·중복 확인 → 태스크/결정/완료 증거로 연결
     → 사용자 승인(우선순위·정책·외부 변경 시)
     → 구현 → 검증 → 장부 정산
```

- 접수: 사용자의 표현을 의미가 바뀌지 않게 짧게 요약합니다.
- 영향 확인: 기존 태스크, 확정 정책, DB·인증·UI·분석 트랙과의 충돌을 찾습니다.
- 연결: 기존 태스크, 신규 태스크, 정책 결정, 완료 확인, 중복 병합 중 하나로 분류합니다.
- 정산: 접수 수와 연결 수를 비교하고 미연결·설명 없는 삭제가 0인지 확인합니다.

## 4. 역할과 승인선

- 사용자: 우선순위, 제품 정책, Production·외부 서비스 변경을 최종 승인합니다.
- 컨트롤타워: 이슈 번호, 의존성, 통합 기준, 검증 증거, 문서 정산을 책임집니다.
- 전담 작업: 배정된 범위만 구현하며 새 발견은 컨트롤타워에 이슈로 반환합니다. 다른 트랙의 정책·외부 설정·Production을 직접 바꾸지 않습니다.

## 5. 병렬 작업 묶음

| 트랙 | 현재 범위 | 내부 순서/경계 |
|---|---|---|
| A · DB 기반 | `TASK-051` 완료 → `TASK-052` → `TASK-053` | 남은 두 작업은 순차. DB 적용 전 읽기 전용 게이트와 별도 승인 필수 |
| B · UI/현장 사용성 | `TASK-050`, `TASK-056`, `TASK-057` | 최신 `main`에서 짧게 작업. DB·인증·분석 로직 변경 금지 |
| C · 분석/진단 | `TASK-038`, `TASK-047`, `TASK-048` | DB 핵심 작업 동안 GA4 확장 보류. 운영 진단은 독립 유지 |
| D · 정책/연구 | `TASK-032`, `TASK-034`, `TASK-039`, `TASK-054`, `TASK-055` | 구현 전 사용자 결정이 필요한 항목을 먼저 분리 |

동시 진행은 핵심 트랙 1개와 충돌이 낮은 보조 트랙 1개를 기본 상한으로 합니다. 같은 파일, 같은 DB 객체, 같은 외부 설정을 만지는 작업은 병렬로 진행하지 않습니다.

## 6. 현재 실행 기준선

1. `TASK-051` 파생 데이터 무결성 migration 002 — Preview·Production·backfill 완료
2. `TASK-052` 라운드 요약 동기화 migration 003 — Preview 완료, Production preflight READY·적용 승인 대기
3. `TASK-053` 첫 화면 조회·누적 통계 장기 확장성 마무리

`TASK-049` 다중 기기 삭제 동기화는 Preview PC↔모바일과 Production 배포·검증까지 완료된 기준선입니다. `TASK-051`은 두 DB 환경의 사후검증 `PASS`와 Production 3라운드·54홀 cache 보정을 재확인했습니다. 사용자 지시에 따라 현재 활성 트랙은 DB뿐이며 다음 작업은 `TASK-052`입니다.

## 7. 장부 정산 시점

정해진 시각 대신 다음 사건이 생길 때 정산합니다.

- 새 작업 묶음을 시작하기 전
- 새 이슈가 3개 이상 쌓였을 때
- Production 또는 외부 설정 변경 전후
- 작업 트랙을 다른 대화나 담당에게 넘길 때
- 작업 종료 또는 새 컨트롤타워 대화로 이동할 때

정산 보고에는 `신규 접수 / 태스크 연결 / 정책 대기 / 중복 / 미연결 / 설명 없는 삭제`의 개수를 포함합니다. `미연결`과 `설명 없는 삭제`는 항상 0이어야 합니다.

## 8. 인수인계 최소 묶음

새 대화와 전담 작업은 내용을 검토하거나 실행하기 전에 다음 **기준선 확인 게이트**를 먼저 통과합니다.

1. 읽기 전용 `git fetch origin`으로 원격 상태를 갱신합니다.
2. 현재 작업 폴더·브랜치·`HEAD`와 `origin/main` 커밋을 함께 보고합니다.
3. 컨트롤타워가 별도 기준을 지정하지 않았다면 권위 기준은 최신 `origin/main`입니다.
4. 작업 폴더가 오래된 브랜치여도 임의 switch·rebase·merge하지 않고 `git show origin/main:파일명`으로 권위 문서를 읽습니다.
5. `PROJECT_LEDGER.md`, `ISSUE_INBOX.md`, `BACKLOG.md`가 없거나 기준 커밋이 다르면 업무를 시작하지 않고 컨트롤타워에 알립니다.

게이트를 통과한 뒤 다음 순서로 읽습니다.

1. `PROJECT_LEDGER.md`
2. `ISSUE_INBOX.md`의 최신 접수·라우팅
3. `BACKLOG.md`의 진행 중·검토·P0/P1 태스크
4. 관련 `DECISIONS.md`, `OPERATIONS.md`
5. `git status`, 현재 브랜치, `origin/main`과 Production 기준 커밋

첫 보고는 `로컬 HEAD와 권위 커밋 / 현재 기준선 / 진행 순서 / 사용자 승인 필요 항목 / 다른 트랙 충돌` 다섯 항목으로 제한합니다. 새 브랜치·worktree 생성은 이 보고 뒤 컨트롤타워 확인을 받고 진행합니다.
