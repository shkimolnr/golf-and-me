# Golf & Me 프로젝트 규칙

이 문서는 프로젝트를 진행하며 지켜야 할 기본 원칙을 정의합니다. 규칙을 바꿀 때는 변경 이유를 `DECISIONS.md`에도 남깁니다.

## 1. 문서의 기준

- `PRD.md`를 현재 제품 요구사항의 단일 기준으로 사용합니다.
- `sources/`의 파일은 참고용 원본이며 수정하지 않습니다.
- 원본과 현재 PRD가 충돌하면 현재 PRD를 따르되, 차이를 `DECISIONS.md`에 기록합니다.
- 확정되지 않은 아이디어는 PRD에 섞지 않고 `BACKLOG.md`의 후보 항목으로 둡니다.
- 날짜는 `YYYY-MM-DD`, 버전은 `v주버전.부버전` 형식을 사용합니다.

## 2. 범위 관리

- MVP의 핵심은 빠른 홀 기록과 라운드 후 분석입니다.
- 기능 추가 전 반드시 MVP 목표 달성에 필요한지 확인합니다.
- Phase 2 이후 기능을 조기 구현하려면 이유와 영향 범위를 결정 기록에 남깁니다.
- 사용자가 명시적으로 확정하지 않은 제품 정책은 가정으로 표시합니다.
- Alpha에서는 "가장 빨리 실행 가능한 앱"을 우선하며, 출시를 막는 정책 토론 대신 되돌리기 쉬운 기본값을 제안·기록하고 구현합니다.
- 외부 연동이나 핵심 흐름에 필요하지 않은 기능은 기본적으로 Alpha 이후로 미룹니다.

## 3. UX 원칙

- 필수 입력을 최소화하고 선택 입력은 나중에도 추가할 수 있게 합니다.
- 홀 입력 순서를 강제하지 않으며 언제든 수정할 수 있게 합니다.
- OB와 페널티 입력 결과는 스코어에 즉시 반영해 보여줍니다.
- OB 재샷 거리는 티샷이면 홀거리, 이후 샷이면 해당 샷의 남은거리로 자동 채우되 사용자 수정값을 우선합니다. 전진 구제 거리는 자동 입력하지 않습니다.
- 라운드 중에는 기록 속도, 라운드 후에는 분석 깊이를 우선합니다.
- 작성 중인 홀에서는 지금 입력할 행을 가장 강하게, 완료 데이터는 그다음으로, 아직 필요하지 않은 행은 가장 약하게 표시합니다. 애니메이션은 짧게 끝내고 모션 감소 설정을 존중합니다.
- 퍼팅 영역은 다음 샷 안내와 무관하게 항상 정상 명도와 조작 가능한 상태로 표시합니다.
- 미완성 홀은 임시 저장할 수 있게 하고, 완료된 홀은 요약 보기에서 명시적으로 수정 모드에 진입한 뒤 변경사항을 저장합니다.
- 모바일 화면과 한 손 조작을 먼저 고려합니다.
- 모바일 중단 후에는 사용자가 직접 홈으로 이동하지 않은 한 마지막 라운드·홀 기록 화면과 작성 초안을 복원합니다.
- 골프장과 전·후반 코스는 세 번째 플레이 홀까지 변경할 수 있고 네 번째 플레이 홀 입력을 시작하면 잠급니다. 초기 기록이 있는 상태에서 구조 정보를 바꾸면 `기록 유지하고 코스만 변경` 또는 `홀 기록 지우고 다시 시작`을 명시적으로 선택하게 합니다.
- 완료 라운드는 골프장과 전·후반 코스를 잠그고 날짜·시간·티그라운드·단위·동반자 메모만 수정합니다.
- 삭제한 라운드는 서버 삭제가 지연되더라도 로컬 삭제 대기 목록으로 가려 다시 나타나지 않게 하고, 연결 복구 후 서버 삭제를 자동 재시도합니다.
- 진행 중 스코어카드의 뒤로가기는 홈 목록으로 이동하고, 상단 라운드 정보 박스에서 정보 수정 화면으로 진입합니다. 완료 스코어카드의 뒤로가기는 라운드 결과로 이동합니다.
- 마지막 홀을 저장해도 자동 완료하지 않습니다. 18홀을 모두 저장한 뒤 스코어카드의 `라운드 완료`와 최종 확인을 거쳐 완료하며, 누락 홀이 있으면 번호를 안내하고 완료를 막습니다.
- 완료한 홀을 수정하면 결과와 통계를 즉시 다시 계산합니다.

## 4. 데이터 및 계산 규칙

- 모든 사용자 소유 데이터는 사용자 ID로 분리하고 접근 정책을 적용합니다.
- 로그인 계정이 바뀌면 이전 사용자의 화면 메모리 상태를 즉시 초기화하고, 사용자별 로컬 저장 키와 서버 프로필을 확인한 뒤 해당 계정 데이터만 불러옵니다.
- 라운드 입력은 로컬에 먼저 저장하고 로그인·서버 연결이 준비되면 Supabase에 비동기 동기화합니다. 서버 오류가 현장 기록을 막아서는 안 됩니다.
- 저장 전 홀 초안도 사용자별 로컬 저장소와 라운드 서버 원본에 함께 보존합니다. 같은 홀의 초안이 여러 기기에 있으면 `draftUpdatedAt`이 최근인 입력을 복원하고, 홀 저장 또는 라운드 완료 시 초안을 제거합니다.
- 오프라인 입력은 기기에 계속 저장하고 연결이 복구되면 별도 조작 없이 서버 동기화를 다시 시도합니다.
- 계정 저장 상태 안내는 기록이 기기에 안전하게 저장됐는지와 사용자가 계속 기록해도 되는지를 함께 설명합니다. 사용자 문구에는 기술 용어인 `동기화`보다 `저장`을 사용합니다. 지연 중에는 작고 지속적인 안내를 유지하고, 복구되면 완료 안내를 잠깐 보여준 뒤 자동으로 닫습니다.
- 같은 라운드의 기기별 저장본이 충돌하면 `updatedAt`이 더 최근인 기록을 사용하고, 서버 RLS로 다른 사용자의 행 접근을 차단합니다.
- 야디지는 선택값이며 값이 없어도 스코어 기록이 동작해야 합니다.
- 검증된 골프장 홀 정보는 단일 시트 누적형 엑셀을 운영 원본으로 유지하고, 앱에는 정규화한 JSON 스냅샷으로 반영합니다.
- 파3 홀의 FIR은 `false`가 아니라 적용 대상 아님(`null`)으로 취급합니다.
- 골프장 홀 정보가 없을 때 파를 추정하지 않으며, 파3·파4·파5·파6 직접 입력을 허용합니다.
- 골프장 홀 정보가 없는 수동 라운드는 전반 1~9번·후반 10~18번으로 생성하고, PAR를 첫 필수 입력으로 안내하며 미입력 홀 거리는 `—`로 표시합니다.
- `[홀 정보]`를 선택한 새 라운드는 시작 전에 골프장·전후반 코스·티·단위를 한 번 확인하고, 이후 정보 수정 저장에는 같은 확인을 반복하지 않습니다.
- `[홀 정보]`가 연결됐어도 선택 티의 공식 거리가 없는 홀은 해당 홀 입력 화면 상단에서 직접 입력을 안내하고 홀거리 입력칸을 강조합니다. 완료 후에는 끝까지 비어 있는 홀 수만 거리 기반 분석 제외 안내에 표시하며, 사용자가 직접 채운 홀은 경고에서 제외합니다.
- 공식 1–18번 전체를 가진 코스는 원본 코스명과 홀번호를 보존하고, 앱에서만 `코스명 OUT`(1–9번)과 `코스명 IN`(10–18번)을 파생합니다.
- 샷별 기록에서는 스코어를 `기록한 샷 수 + 퍼팅 수 + 벌타`로 계산합니다.
- 퍼팅 입력 전에는 마지막 샷의 도착 위치를 추정하지 않습니다. 퍼팅 시작 위치가 그린이면 마지막 샷을 `온 그린`, 엣지면 `엣지`, 퍼팅 0회면 `홀인`으로 판정합니다.
- 엣지에서 퍼터로 친 첫 타는 총 스코어에는 포함하되 공식 퍼팅 수에서는 제외합니다.
- 퍼팅 수 원본에는 임의의 최대값을 두지 않습니다. 경기 방식이나 핸디캡 제출용 상한은 원본 기록을 보존한 별도 계산값으로 다룹니다.
- 페널티 구역은 `페널티`로 표시하고 발생 1회당 +1벌타로 계산합니다.
- OB 재샷은 실제 재샷을 샷으로 기록하고 +1벌타를 더하며, 전진 구제는 +2벌타를 더합니다. 실제 샷 수와 벌타를 분리하여 중복 계산하지 않습니다.
- 통계의 분모와 결측값 처리 규칙을 화면 및 코드에서 일관되게 유지합니다.
- 총 타수는 점수가 있는 홀을 합산하지만 파 대비와 스코어 분포는 점수와 PAR가 모두 있는 홀만 사용합니다. FIR은 값이 기록된 파4·파5, GIR과 퍼팅은 해당 값이 기록된 홀만 분모로 사용하며 표본이 없으면 `—`로 표시합니다.
- 미입력 거리와 클럽은 0 또는 실패로 해석하지 않습니다. 향후 거리·클럽 통계에는 실제 기록 표본 수와 기록 완성도를 함께 표시합니다.
- 클럽 비거리는 클럽별 독립 최신값이 아니라 특정 시점의 골프백 전체 세트로 저장합니다. 일부 클럽만 변경한 경우 같은 단위와 거리 기준의 직전 값을 이어받아 완전한 세트를 만듭니다. 저장 직후에만 새로 입력한 클럽을 연한 초록 배경으로 표시하고, 재진입 시에는 강조를 제거하며 `NEW` 문구는 쓰지 않습니다.
- 캐리 거리/런 포함 기준 또는 M/YD 단위가 바뀌면 직전 숫자를 자동 승계하지 않습니다. 새 기준에서 입력하지 않은 클럽은 `null`로 유지합니다.
- 클럽 표시 순서는 사용자가 선택한 시점과 무관하게 드라이버 → 우드 번호순 → 유틸리티 번호순 → 아이언 번호순 → 웨지(P/A/S/L 후 로프트 번호순)로 고정합니다. 비거리 4열 표는 왼쪽 열을 위에서 아래로 채운 뒤 오른쪽 열로 이어집니다.

## 5. 개발 원칙

- 하나의 작업은 하나의 명확한 완료 조건을 가집니다.
- 데이터 모델 변경에는 마이그레이션과 롤백 방안을 포함합니다.
- 인증, 권한, 스코어 계산에는 자동 테스트를 우선 작성합니다.
- 비밀키와 개인 정보는 저장소에 커밋하지 않습니다.
- 접근성, 로딩, 빈 상태, 오류 상태를 정상 화면과 함께 구현합니다.
- 구현 완료 전 모바일 크기에서 핵심 사용자 흐름을 검증합니다.
- 공개 배포 전 `OPERATIONS.md`의 배포·인증·백업·삭제·실기기 점검표를 확인합니다.

## 6. 작업 상태

`BACKLOG.md`에서는 다음 상태만 사용합니다.

- `대기`: 아직 시작하지 않음
- `진행 중`: 현재 작업 중
- `검토`: 구현 완료 후 확인 필요
- `완료`: 완료 조건과 검증을 모두 통과
- `보류`: 외부 조건 또는 결정이 필요

동시에 `진행 중`인 최상위 작업은 가급적 하나로 유지합니다.

## 7. 완료 정의

작업은 다음 조건을 모두 충족할 때 완료로 표시합니다.

- 요구사항과 수용 기준을 충족함
- 관련 테스트 또는 수동 검증을 통과함
- 오류·빈 상태가 처리됨
- 관련 PRD, 백로그, 결정 기록이 최신 상태임
- 새로운 비밀 정보나 개인 정보가 포함되지 않음

## Decision Policy

When requirements are ambiguous, propose a reasonable default and continue implementation.

Record assumptions in code comments or PR notes when necessary.

Avoid blocking development for minor policy decisions.

## Documentation Rules

Never invent dates.

Use the current date when required.

If the current date is unknown, explicitly ask or leave it blank.

## Documentation Rule

When implementation changes the approved plan,
propose documentation updates before proceeding.
